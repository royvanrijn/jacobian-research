# Corrected good-prime scoring in fresh higher MW16 territory

**Frozen finite campaign. The exact correction and scanner cost gates pass.
All 320 signed-frame and top-seven checks, the 3,059,269,468-address scan and
all 1,310,720 retained-score replays pass. The independently reconstructed
10,240 scalar models are distinct and have strict initial retention coverage.
All scalar checks, sixty-finalist selection and disjoint validation pass.
All sixty point attempts, 2,580 completed boxes, exact admission histories,
rational geometry and full-cloud checks modulo 2, 3 and 5 pass. The terminal
aggregate certifies seventeen added directions across five curves, including
one new rank-at-least-23 curve. All 182 standalone stages pass, as do its
minimal-model proof, Sage export and the full 199-curve inventory replay.**

The completed [rank and equation certificate](../../artifacts/generated-results/elliptic-curves/corrected60_mw16_results_v1.json)
and [experiment aggregate](../../artifacts/generated-results/elliptic-curves/corrected60_mw16_experiment_v1.json)
report these five positive gains over the respective independent generic16
subgroups:

| Family | Parameter | Certified rank lower bound | Added directions |
|---|---:|---:|---:|
| 01 | -28099/22207 | 17 | 1 |
| 02 | -19745/6344 | 19 | 3 |
| 03 | -31993/347 | 18 | 2 |
| 04 | 28195/1219 | 23 | 7 |
| 04 | -28097/5460 | 20 | 4 |

The other fifty-five curves retain a certified lower bound of sixteen.
All sixty equations are mutually nonisomorphic over Q and unmatched among
593 pinned catalogue equations and 1,285 previously measured equations.
This establishes catalogue-relative novelty, not universal novelty or exact
ranks. The completed boxes do not prove absence of further directions.
The rank-at-least-23 curve is now stable inventory ID `new-20260906-199`.
Its proved global minimal equation has Weierstrass coefficients

```
[0, -1, 0,
 -291407592487661064311830568265477746905095407334785,
 1703555122640705180225385929075057598764309363718890344075866794520163078725]
```

All 23 independent points transport exactly to this model. Its invariant gcd
is 64, and the exact local scaling exclusion proves global minimality.
The [minimal-model proof](../../artifacts/generated-results/elliptic-curves/corrected60_high_rank_minimal_proof_v1.json)
and [executable Sage equation/point export](../../artifacts/generated-results/elliptic-curves/new_corrected60_high_rank_curves.sage)
replay and execute successfully. No exact conductor is asserted.

The [standalone replay](../../artifacts/generated-results/elliptic-curves/corrected60_mw16_point_portable_replay_v1.json)
passes all 182 stages from a 57,269,213-byte archive. The
[V19 inventory](../../artifacts/generated-results/elliptic-curves/new_high_rank_curve_index_v19.json)
now contains 199 mutually nonisomorphic curves, preserving all prior IDs:
eight with bound 27, eighteen with 26, thirty-seven with 25, fifty with 24,
forty-six with 23 and forty with 22. All 199 independent-point certificates,
catalogue exclusions, exact distinct j-invariants and the equation CSV pass
[the inventory replay](../../artifacts/generated-results/elliptic-curves/new_high_rank_curve_index_v19_memory_replay_v1.json).

The correction was necessary arithmetic. This campaign's seventeen gains
and the earlier broader campaign's thirty-six gains concern different
parameter populations; they do not measure the causal effect of the score
correction or settle which score level earns more search budget. The
[matched retained-score experiment](RETAINED_MW16_SCORE_STRATA_2026-09-06.md)
addresses that separate production question under identical point exposure.

The [local-reduction audit](MW16_OMITTED_GOOD_PRIMES_2026-09-06.md) proves
that displayed singular models omit some intrinsic good-prime terms at
5 and 13. Complete local tables modulo 125 and 169 restore those terms.
All 1,660 cells and 10,240 full-height scalar-model comparisons replay.
The separate corrected scanner passes three regression groups, twenty
complete real-cache frames, a 6,379,630-address cost gate in 30.2566 seconds,
and independent checks of every retained score.

This campaign applies that correction before retention while moving to a
new parameter population. It preserves the earlier
[narrow trial](MW16_JOINT_HIGHER_ANNULI_2026-09-06.md) and
[broader displayed-score trial](BROAD_MW16_HIGHER_POPULATION_2026-09-06.md).
It is a further prospective experiment, not an alteration of their cohorts
or a claim that corrected scores predict ranks better.

## Frozen territory and allocation

The two bands remain 16384<H<=65536 and 65536<H<=262144, with
H=max(|n|,d), gcd(n,d)=1 and d>0. Each of five families receives sixteen
denominator residues per sign and band, modulo 256 and 4096 respectively.
A fixed SHA256 seed orders eligible residues of the prescribed parity.
The original outer residue, narrow-trial residue and all sixteen prior
broader residues are excluded in every group.

The resulting 320 slices cover **3,059,269,468 primitive addresses**.
Exactly one slice is the already fixed corrected-scanner benchmark. Its
unchanged scanner transcript is reused once, with exact command and source
bindings. The other **319 slices contain 3,052,889,838 new addresses**.
No full benchmark scan is repeated. These are parameter addresses;
rational-isomorphism novelty remains a separate later check.

Each address receives all 3,510 selection primes through 32749, including
the restored good 5/13 terms, before retaining 4,096 per signed slice.
The fixed 1,310,720 retained candidates form 131,072-candidate pools per
band/family. Exact within-prospective-roster deduplication admits 1,024
scalar candidates per pool: 10,240 total. Fresh scalar traces through
65521 select six finalists per pool, sixty total. Wholly disjoint primes
65537 through 131071 validate the frozen finalists without changing them.

## Gates and exact evidence

All fifteen base/local binary caches must match their existing exact byte
proofs. Complete signed actual-modulus frames and top-seven orderings are
checked for all 320 slices. The fixed lower-band benchmark supplies its
45-second cost gate; the first higher-band slice must pass the same gate.
Four scanner workers, 120 seconds per call, a 7,200-second run cap and
1,800-second replay cap apply. Per-slice immutable files retain every
selected address and score component. Independent short/extended cache
readers plus exact full-model scaling and finite-field point counts check
all retained corrections. Displayed short scores and restored contributions
are stored separately alongside the corrected total.

The [independent model and retention audit](../cas/audit_corrected_mw16_score_coverage.py)
reconstructs every scalar model directly from its homogeneous coefficients.
All 10,240 have distinct j-invariants. In each of the ten band/family groups,
the last admitted scalar candidate is strictly ahead of all 32 signed-slice
retention boundaries in the first three ordering fields. Thus no address
discarded by the 4,096-per-slice cut can precede that group's admitted prefix
under the frozen initial score. All saved displayed and restored components
agree with the corresponding retained records. The
[certificate and read-only replay](../../artifacts/generated-results/elliptic-curves/corrected_higher_mw16_score_coverage_v1.json)
pass. This is finite initial-score coverage, not a rank predictor or a claim
of globally optimal selection through 65521.

The later budgets remain fixed: four scalar workers, 20 seconds per case,
80-row checkpoints, a first-twenty cost gate and 2,400-second run cap.
Every model's first cached extension and two independent character sums
must agree before sixty finalists and disjoint validation.

Point searches use only the sixteen generic sections and 43 fixed generic
parity labels per fibre, height 125000 and ten seconds per chart. Every map
must pass before any point search. Two workers, 600 seconds per fibre and a
provisional 28-direction stop apply, with at most 2,580 boxes. There is no
adaptive wave, refill, replacement slice or retry.

Exact histories, rational geometry, generic transports and full-cloud
independence proofs precede catalogue access. Finalization additionally
waits for the preserved broader cohort, then compares against the pinned
593 catalogue equations and 1,285 prior equations. A separate point-only
bundle requires 182 isolated replay stages. No known-record equation,
parameter, point, rank, target j-invariant or jump label enters selection
or point execution.

Sources: [scanner](../cas/scan_corrected_mw16_higher_annuli.py),
[scalar selector](../cas/score_corrected_mw16_higher.py), their
`finish_corrected_mw16_higher` controllers, and the `corrected60_mw16`
point/proof sources. Protocols and checkpoints are under
`corrected-mw16-higher-annuli-v1`, `corrected-mw16-higher-scores-v1` and
`corrected60-mw16-pari-v1` in local artifacts.

## Bounded conductor check

The [fixed local conductor audit](../../artifacts/generated-results/elliptic-curves/corrected60_conductor_bounds_v1.json)
and [independent PARI replay](../../artifacts/generated-results/elliptic-curves/corrected60_conductor_bounds_pari_replay_v1.json)
pass for all nine local prime/curve pairs on the new rank-at-least-23 fibre.
They cover 2, 3 and every discriminant prime through 10000, with one worker,
a 120-second cap per stage, and no larger-factor search. Sage's generic Tate
algorithm and PARI agree on local reduction types and conductor exponents.

The resulting conductor upper bound has 444 bits; the smallest recorded
conductor among the pinned catalogue's curves of rank at least23 has 276 bits.
This bound gives no improvement. A 122-digit residual cofactor remains
unfactored and the exact conductor remains UNKNOWN. An upper bound above
the recorded minimum does not exclude a smaller true conductor. No point
budget or retained-score allocation was changed by this local check.
