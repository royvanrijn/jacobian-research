# Fixed outer-parameter R17 trial

The H131072 stratified scan and all 5798 outer-candidate extended-prime scores
have completed and replayed. The selected 48 equations are distinct up to
rational isomorphism and exclude all 741 previously measured equations.
All 48 exact map sets passed before any point search. All2160 point boxes, exact histories and full-cloud checks modulo2,3,5
now pass. Every one of the48 curves still certifies17; the2223 retained point
witnesses give no higher certified bound. No curve enters the high-rank inventory.

## Mathematical and computational gate

The native11952 known rank-at-least29 control has exact compact parameter
89074/31895, outside the earlier H32768 envelope and inside H131072. Its fixed
generic17 detector recovers rank at least28 at point height125000. This supplies
a reason to test a larger parameter envelope with the existing detector; it
is not a rank-density prediction or universal sensitivity calibration.
The [public coordinate audit](../../artifacts/generated-results/elliptic-curves/public_compact_parameter_heights_v1.json)
records the exact parameter transport. Public points and ranks do not enter
candidate scoring, equation selection or the new map construction.

The scan fixes one denominator residue modulo1024 in each of twelve family/sign
slices, derived from the salt `elliptic-stratified-height-131072-v1`. It covers
122433806 primitive addresses, retaining512 per slice with the existing562
cached primes through4093. The entire saved6144-row scan completes in24.122259
seconds; its first full slice passes the15-second cost gate. All24 small
reference/optimized scanner calls agree, and the saved population replay passes.
The failed version-one path setup is preserved separately; no scan ran in it.

Exactly5798 retained rows have height above32768. They receive the existing
scores through32749 and disjoint validation32771through65521. Six benchmark
curves pass direct character-sum and timing checks. The full trace stage takes
531.925107 seconds and its replay30.677241 seconds. Eight candidates per family
are selected by score, good-prime count, denominator and signed numerator,
after equation-only exclusions. Validation never enters ordering. The selected
parameter heights range from45165 to129627. No failure or later catalogue match
is refilled.

## Frozen point exposure and evidence

Each curve starts from the17 generic sections and all43 or49 exact generic
maximum-parity classes. Numerical384-bit heights and CVP choose representatives;
parities, rational centres and quartic transports are checked exactly. Numerical
choices are not covering or optimality certificates. Every map file precedes
the first point attempt.

The fixed cap is2160 initial charts, each with height125000 and ten seconds.
There are two point workers, with600 seconds per curve. A curve stops at a
provisional28-point subgroup pending independent replay. There is no adaptive
wave or automatic retry. Full-cloud independence modulo2,3,5, rational map and
raw-point provenance, and the pinned593-equation catalogue comparison follow.
Neither scores nor a point count alone prove a rank lower bound.

Frozen sources are `scan_outer131072_r17_v2.py`, `extend_outer131072_r17.py`,
`outer48_r17_pari_batch.py` and their map/verifier modules under `../cas/`.
The [completed experiment](../../artifacts/generated-results/elliptic-curves/outer48_experiment_v1.json)
and [point proofs](../../artifacts/generated-results/elliptic-curves/outer48_r17_results_v1.json)
record the null bounded result. Isolated portable replay is not yet claimed.
Local immutable protocols and ledgers are under `outer131072-r17-stratified-v2`,
`outer131072-r17-extended-v1` and `outer48-r17-pari-v1` in the local elliptic
artifact directory. The current inventory and theorem status remain in
`MATH_STATUS.json`; this completed experiment supplies no new inventory entry.

## Retrospective retention diagnostic

After the trial closed, one exact known29 control-score call and four independent character-sum checks passed. Against exactly the same967 saved11952 outer candidates, the control has hypothetical insertion position435 under the short score and2 under extended S1. The [same-population comparison](../../artifacts/generated-results/elliptic-curves/outer_known29_retention_comparison_v1.json) replays both orderings. Its positive denominator residue is150 modulo1024; the frozen scan chose754, so the control was outside the sampled population.

This is a concrete short/extended ordering mismatch, conditional on the retained population. It is not a full-population quantile, a prospective success rate or a proof that an enlarged global short-prefix cutoff would discard that curve. The score and point trial remain immutable. An eight-prime performance prototype tests the cost of caching extended projective traces before any wider retention or parameter campaign; no full cache or new search follows automatically.
