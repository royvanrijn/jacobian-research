# One fixed follow-up using the new compact candidates' own subgroups

All245 declared point boxes complete on the five new26-point curves, stable
IDs102through106. Every final cloud still certifies26 modulo2,3,5. Exact
histories and rational geometry pass. This single follow-up certifies no stronger rank lower bound and adds no
inventory entry.

The [aggregate certificate](../../artifacts/generated-results/elliptic-curves/compact192_specialized_followup_v1.json)
binds742 retained point witnesses. All five map files preceded every point
search. The canonical execution record is
`artifacts/local/elliptic-curves/compact192-specialized-followup-controller-v1/ledger.json`.
An isolated replay of this additional follow-up is not yet claimed.

The initial [compact192 experiment](COMPACT192_UNSEARCHED_TRIAL_2026-09-06.md)
uses centres from each curve's original17 generic points. A newly certified
26- or27-point subgroup supplies additional directions that those initial
centres did not use. This follow-up tests that specific point-visibility gap.
It does not test population density or prove solubility of an unsearched cover.

The [known control](SPECIALIZED_PARITY_CONTROL_2026-09-06.md) recovered its
29th independent direction from28 known points using a fixed2048-mask
specialized-parity sample at height125000. That supplies a finite recovery
and cost gate. It does not establish prospective sensitivity. The previous
six-curve rank27 trial's null outcome remains retained.

## Frozen selection and limits

Select every new V13 entry beyond V12 whose source is
`compact192_r17_results_v1.json` and whose certified lower bound is26or27,
ordered by stable local ID. Require the entire eligible roster to have1through24
curves; stop instead of truncating if that limit fails. No public points,
oracle words, parameter scans, score changes or result-dependent refills enter.

For each seed, take the first2048 distinct SHA256-derived masks with nonzero
quotient above its generic17-point prefix. Numerical384-bit heights and a
unimodular LLL change choose representatives; their parities and rounded
quadratic norms are checked exactly. Select49 largest computed norms, with
mask ties. Every map in the entire cohort must precede every new point search.

The sole follow-up permits49 charts per curve, height125000 and ten seconds
per chart, with one point worker and target28 pending exact replay. The absolute
cap is1176 boxes, narrowed to49 times the actual roster at preparation.
There is no second wave, larger height, automatic retry or inventory promotion.
Failures and censored records are preserved.

## Evidence required before promotion

`compact192_specialized_followup.py` freezes the actual roster, runs the point
attempts and replays their exact admission histories.
`audit_compact192_specialized_followup.py` checks all retained point clouds
modulo2,3,5. `replay_compact192_specialized_geometry.sage` checks exact
parity/norm transports and rational map/point provenance. The controller stops
for review if an odd-prime certificate gives a stronger bound than modulo2.

The numerical representatives are not certified optimal or a complete coset
cover. A bounded miss proves no point absence, rank upper bound or saturation.
Any successful lower bound still needs separate inventory and portable-evidence
promotion; catalogue-unmatched does not mean universally novel.
