{
  "schema": "rank-jump.completed-cohort-panel-protocol.v1",
  "question": "Do the newer completed high/zero-gain controls require a change of cubic Galois structure or a small shared halving field?",
  "cohorts": ["compact192_r17_results_v1.json", "extended20_mw16_results_v1.json", "new_mw16_followup_results_v1.json"],
  "limits": {"observations": 215, "initial_observations": 212, "adaptive_observations": 3, "modular_prime_bound": 503, "seconds_per_command": 30, "new_parameters_or_point_searches": 0},
  "pairing": "For each R17 frame containing an initial rank26 fibre, choose a rank26/observed17 pair minimizing max(parameter heights)/min(parameter heights), then parameter distance and identifiers. Same frame and initial exposure; no score matching is asserted.",
  "checks": "Strip and freeze exact equations, ordered generic prefixes, independent witness bases, proof-prime rosters and completed-box counts. Recompute membership, finite Kummer rank and the cubic Galois group. Keep all three adaptive observations separate from initial exposure.",
  "hypothesis_test": "A smaller-than-S3 cubic group at high gain would leave a Galois-splitting explanation open. Uniform S3 across high/zero controls refutes its necessity. Independent quotient Kummer directions invoke the existing relative halving-field theorem; they do not exclude other common soluble objects.",
  "boundary": "Retrospective denominator with measured lower bounds, not exact ranks or an unbiased rank distribution. No active selector, scoring policy, worker, cohort, output or mathematical status is changed."
}
