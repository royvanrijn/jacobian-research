# Research status

C24 remains the sole active constructive branch.  The C22
quadratic-remainder associated graded has now failed at `k=2`, so the
all-`k` transfer programme and its dependent claims are archived.

| Claims | Workflow state | Meaning |
|---|---|---|
| C01--C04 | Being written up | Consolidation into the core paper; no theorem extension |
| C05--C16 | Awaiting review | Proofs are frozen pending specialist review |
| C17--C20 | Frozen examples | Only the exact low-`k` local algebras remain active as examples |
| C18, C21 | Archived | Their all-`k` transfer input was not recovered |
| C22 | Archived / refuted mechanism | The proposed transverse associated graded fails at `k=2` |
| C23 | Independent / scoped | Cubic classification and `(2,3)` obstruction proved; higher degrees open |
| C24 | **Active** | The sole active theorem programme |

## C24 next milestone

**Prove a coordinate-independent invariant showing when two
resolvent-cancellation constructions cannot be polynomially left--right
equivalent.**

The current finite-normalization signature separates C24 with `r>=2` from
generic weighted seeds. The unresolved test case is `r=1`, `m>1`, where the
known degree, monodromy, divisorial ramification, residue degrees, and generic
sheet-loss data agree. The next work should compare conductor ideals,
boundary-intersection local rings, or filtered valuation semigroups. No other
claim should be extended while this milestone is active.

## Archived transfer conclusion

The divided-power ribbon norm misses `X^2`, and the later quadratic-remainder
proposal misses `X^3` already at `k=2`.  The latter proposed special fiber is
positive-dimensional for every `k>=2`, so it cannot be the tangent cone of
the finite low-degree transfer algebras.  See the exact
[counterexample](experimental/transfer-and-cancellation/QUADRATIC_REMAINDER_ALGEBRA.md)
and the preserved [archive](archive/transfer-all-k/).  No C22-to-C18-to-C21
dependency chain is active.
