# Temporarily disabled workflows

These GitHub Actions definitions are intentionally outside `.github/workflows`
while the research-tree relocation is being stabilized. They remain preserved
for review and can be re-enabled by moving the required files back after their
paths and verification assumptions have been checked.

This does not disable local verification targets; use the forwarding root
`Makefile` or run `make` from `research/` for targeted checks.
