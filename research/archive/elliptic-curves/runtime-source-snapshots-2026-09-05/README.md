# Generation-time runtime sources

Historical source bytes pinned by existing certificates before shared-runtime
migration. Each snapshot is addressed by its SHA-256 hash; `index.json` records
the original path and revision. These files preserve provenance only. Use the
active exact witness replayers for verification; use the pinned revision for
optional full regeneration. Mathematical status remains in `MATH_STATUS.json`.
