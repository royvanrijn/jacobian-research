# Completion audit for Zenodo 21479814

This ledger maps the original mission to concrete repository artifacts.  It
also records limitations that must not be hidden by the phrase “locally
reproduced.”

| Objective | Status | Evidence / limitation |
| --- | --- | --- |
| Exact source provenance | complete | [PROVENANCE.md](PROVENANCE.md) pins the sole Zenodo attachment, PDF, source snapshot, versions, relations, licenses, and hashes. |
| Exact theorem and scope | complete | [DEGREE_FRONTIER_125.md](DEGREE_FRONTIER_125.md) quotes the manuscript theorem and conditional corollary and states the characteristic-zero, larger-coordinate interpretation. |
| Arbitrary-to-standard reduction chain | complete as an external reduction audit | Every implication is tied to the labelled GGV/GGHV theorem used; this repository does not reprove those long normal-form theorems. |
| Historical frontier | complete with a disclosed source-access qualification | The 2014--2022 primary sources were read in full.  Heitmann's primary text was inspected; Moh's publisher exposes only the opening scan without authentication, so later section claims are cross-checked through exact citations and caveats in the later primary papers. |
| \((72,108)\) elimination | complete | [PAIR_72_108_REPRODUCTION.md](PAIR_72_108_REPRODUCTION.md) reconstructs supports, bracket layers, branches, fields, ideals, and final identities.  Case 1 is correctly described as a complete necessary truncation, not a full infinite-band system. |
| Primary exact CAS replay | complete | The pinned `exact_replay/verify_all.sh` run ended `JC2_72_108_EXACT_REPLAY_PASS`; commands and versions are in [cas/README.md](cas/README.md). |
| Independent certificate | complete | [cas/verify_h_certificate_independent.py](cas/verify_h_certificate_independent.py) independently checks the hard identity and now rejects any system or certificate input whose SHA-256 is not pinned. |
| Division and saturation audit | complete | Section 7 of the reproduction note accounts for every inverse, factor split, discarded equation, and the complementary \(h=0\) strata. |
| Status and source integration | complete | `STATUS.md`, the root index, reproduction guide, marked-point note, source index, and search policy are updated with dimensionally scoped language. |
| Newton--boundary dictionary | complete as a qualified comparison | [NEWTON_BOUNDARY_DICTIONARY.md](NEWTON_BOUNDARY_DICTIONARY.md) marks exact, standard, interpretive, and speculative correspondences. |
| Conceptual replacement | partial positive prefilters; no replacement proof | Complete `A2` intersection packages now pass an intrinsic adjunction/Noether gate; a target pole vector reconstructs the ordinary and log ramification vectors and selects dicriticals, proving the canonical free-depth-three obstruction.  Certified branch scales compile to complete toroidal proximity graphs, boundary/intersection matrices, local differents, conductors, and Smith invariants.  The weighted three-band Poisson-square locus has three reduced components with generic scheme multiplicities `2,3,1`; their pairwise reduced intersections have branch counts `(2,2,1)` and explicit dense charts with tangent dimensions `(8,7,6)`.  The two extra branches have distinct chosen length-five kernel slices with Hilbert/socle data `(1,4;4)` and `(1,3,1;1)`.  The exact identity `I:d0^infinity=I` proves that `d0` is a non-zero-divisor.  On `d0=1`, a `d3,d2` colon filtration proves that the full scheme has exactly eight associated primes: three minimal components, three embedded intersection branches, and two deeper core/intersection branches; the second gluing layer has two primary curve components.  Completed local presentations on those five embedded strata remain open.  The primary `(72,108)` proof supplies smooth-branch rays `(2,1),(3,1),(4,1)`; the Laurent maps have adapted base ideals `(t,x^4),(t,x^6),(t,x^8)`, and their source graphs compile in `4,6,8` blowups.  Additive composition gives one eight-blowup graph.  The final `F_4` involution swaps the base divisors; filling pre-transition `Xinf`, which is post-transition `X0`, gives a unimodular `10 x 10` affine-plane boundary matrix passing adjunction.  The full common-graph pole vector is `(1,24,1,1,1,12,9,6,3,2)` and has no dicritical.  The first weighted-Wronskian equation forces the actual source cluster at `E3 intersect E4`: one base-order-10 blowup of pole `3`, followed by ten simple pole-`2` children.  It excludes the numerical smooth-`E3` candidate.  The final plane-return edge forces `A=a*r^2,C=c*r^3` with quartic `r`; all five homogeneous root-partition models pass the intrinsic gates with remaining self-intersection `29`.  The primary split-factor formula further forces `r=(s-beta)^4` in Case 1, and the alternate order-three chart supplies an exact adapted transverse parameter.  Both terminal cases therefore select the same 23-component numerical package with a degree-12 dicritical.  Correcting the transformed bracket `[P,Q]=X^2` changes their actual dicritical normal indices to `3` and `5`; their exact residue cover degree is a priori `1,2,4`.  General polynomial decomposition excludes Case-2 cover degrees `2,4` before `J0` or the residual `J1` compatibility equations, leaving only the birational row and valuation remainder `24`.  The bottom equations separately reduce to a derivative-gcd equation and force `t|gcd(C',G')`, excluding gcd degree zero.  A pinned four-equation unit ideal also excludes maximal gcd degree seven without residual `J1` compatibility, leaving exact degrees `1,...,6`.  Case-1 composition cannot yet be audited because its alternate residue depends on lower bands omitted from the necessary truncation.  The chain-to-boundary gap is closed, but the permissive edge-only family proves that no full intrinsic replacement of the coefficient certificate has yet been obtained. |
| Frontier 125--150 | complete as a deterministic published-table regression | [NEXT_DEGREE_FRONTIER.md](NEXT_DEGREE_FRONTIER.md) records all 13 pairs and all 24 chain realizations.  It does not claim to independently reimplement the complete-chain generator or to have derived the new Laurent systems. |
| Family extension | compiler middle end tested; no family theorem | Ratio \(2:3\) and gcd data do not suffice.  The \((72,108)\) six-equation/de Rham identification now has an explicit tail-basis certificate and the engine computes Gauss--Manin matrices.  The proposed \((96,144)\) repeated-tail row has a source-level conflict with the 2016 lower-side exclusion and no derived Laurent bands. |
| Search-policy consequence | complete | [SEARCH_POLICY.md](SEARCH_POLICY.md) requires admissibility and boundary filters before coefficient solving and reserves sub-frontier searches for tests and automorphisms. |

**Case-2 supersession (2026-07-23).**  The conceptual-replacement row above
stops at the birational residue because it records the earlier staged audit.
The remaining row is now excluded: the seven residual `J1` compatibility
cubics, localized at the forced endpoint `G_12 != 0`, generate the unit ideal
over the exact degree-35 field without `J0`.  Thus no Case-2 cover or gcd row
remains live; only the three Case-1 cover rows await missing-band recovery.

Accordingly, the degree-125 consequence is locally reproduced in the precise
conditional sense used by this directory.  The next research objectives are
reconciling the 2016 exclusion of the \((8,28),(8,40)\) boundary data with the
2017 raw complete-chain table, then deriving Laurent systems only for rows
that survive that audit.  The compiler will not infer a de Rham block from
corners alone.
