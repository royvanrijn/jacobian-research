# Curve92 productive-anchor search: complete, no certified gain

**Independent replay passes.** The native MW16 curve at3161/432, inventory
`new-20260906-92`, remains at certified lower bound26 after all312 planned
invocations. No invocation timed out. The full retained cloud certifies26
modulo2,3,5. This is not an exact-rank proof or absence of a27th direction.

The search supervisor takes247.423 seconds and independent replay51.002
seconds. Native preparation takes7.228 seconds. The landscape scores3072
cosets over three productive generic anchors, selects156 centres and takes
15.541 seconds. Both quartic-minimizing and factor-free coordinate policies
execute on every selected centre.

## Input and proof boundaries

The starting26-point subgroup and generic sixteen are reconstructed and
certified on their exact native model through the compact atlas and retained
constant scaling. Both prior point attempts are replayed through complete
finite reductions to identify every productive generic class. The resulting
anchor masks are15341,25820 and29806.

Only these three generic CVPs are needed for this declared subset. Each is
solved with integer-scaled LDL and independently with the original rational
solver. No floating census completeness claim or full generic shell
enumeration is substituted for those checks.

The search uses the same bounded productive-anchor method as
[curve90](CURVE90_PRODUCTIVE_V3_SEARCH_2026-09-09.md): cached finite admission,
two exact coordinate maps, per-chart receipts, one whole-cloud audit per
epoch, immediate rebuilding after a certified gain, and full reference
selection/point replay. Its frozen limits are4096 actual invocations,
height125000,10 seconds per invocation, target lower bound32, at most seven
epochs, and separate7200-second/3-GiB search and replay supervisors. The
initial finite schedule exhausts without triggering another epoch.

The exact conductor would improve the retained reported rank27 benchmark by
about15.7 times **if** a27th direction were proved. That condition was not
achieved; no conductor record is announced. A separate diagnostic computes
global root number+1 from the complete certified bad-prime list. It supplies
no algebraic-rank upper bound or extra-point certificate.

## Evidence

- [Completion records and hash index](../../artifacts/generated-results/elliptic-curves/curve92_productive_v3_v1/result.json).
- `artifacts/local/elliptic-curves/curve92-v3-preparation-v1/`: native seed,
  historical gain proofs, generic CVP comparison and productive bank.
- `artifacts/local/elliptic-curves/curve92-productive-v3-discovery-v1/`:
  protocol, raw receipts, complete-cloud audits, terminal, independent
  `verified.json`, frozen source archive and root-number diagnostic.
- Sibling `-supervision` and `-replay-supervision` directories: terminal
  process identities, logs, elapsed times and resource accounting.
- [Preparation](../cas/prepare_curve92_productive.py) and
  [bounded runner/replay](../cas/run_curve92_productive_v3.py).

No old search was repaired or restarted. No mathematical-status rank
promotion is warranted by this bounded no-gain result.

## Next scheduling diagnostic

The exact-conductor database has expanded to129 rows. A cheap local
root-number product audit identifies four rank25 rows whose root signs differ
from the parity of their known lower bounds. This is a scheduling clue only;
it does not establish the parity of algebraic rank or the existence of points.

The next candidate is `new-20260906-200`, the same MW16 family02 at
`-32999/14074`, with certified lower bound25 and root number+1. Its `j` has no
match in the pinned fresh682-curve catalogue. Its source packet is
`strata60_mw16_results_v1.json`, curve index57. The root audit and its explicit
limits are in `artifacts/local/elliptic-curves/future-root-prior-v1/report.json`.
No curve200 search is part of this completed curve92 roster.

## Fresh pairwise-parent first pass

The later short-pass programme returns to the certified M26 with new parent
classes, preserving the completed312-chart productive search. All pairwise
XORs of its three productive masks give three distinct new classes:
4274 at generic norm3.5,20355 at norm4, and24369 at norm5.5. These exact minima
are independently replayed in0.956 seconds under120 seconds/1GiB. The original
seed and generic metric are rechecked; no higher point enters selection.

Preparation is sealed under
`artifacts/local/elliptic-curves/curve92-pairwise-preparation-v1/` by
[the pairwise adapter](../cas/prepare_pairwise_parent_bank.py). The planned
first pass uses the unchanged [100-invocation wrapper](../cas/run_short_seed_v3.py),
both maps at height125000,10 seconds per invocation, immediate rebuild after
a certified gain, and separate1800-second/3-GiB search/replay bounds.
The pass launches after the curve90 replay completes successfully, under
`artifacts/local/elliptic-curves/curve92-pairwise-short-v3-discovery-v1/`.

The motivating endpoint is one new independent direction beyond M26 and then
a fresh exact comparison against the reported rank27 conductor benchmark.
The retained conductor inequality is a payoff if a gain occurs, not evidence
that a gain exists. A new parent bank does not imply a new point.

The first pass completes100 invocations, with no timeouts or certified gain:
M26→M26. All146 selected centres are disjoint up to sign from the previous156
on the identical ordered basis. Complete-cloud finite ranks modulo2,3,5
remain26. The terminal reason is `CHART_BUDGET_EXHAUSTED`; the remaining
suffix is untested. Search takes95.416 seconds, peak RSS325152768 bytes.
Independent replay passes in53.632 seconds, peak RSS302432256 bytes.

The [sealed result](../../artifacts/generated-results/elliptic-curves/curve92_pairwise_short_v3_v1/result.json)
includes the full basis and completion records. The exact comparison is
retained in `initial-centre-disjointness.json`. The hash-bound
`continuation-queue.json` preserves centre50, quartic-minimized map, as the
next policy to consider. No rank27 direction or conductor improvement was
found by this prefix. No curve-rank upper bound follows.

## Continuation with a verified landscape cache

[The cached continuation wrapper](../cas/run_cached_seed_v3.py) creates a new
directory from the independently verified100-chart pass. It checks the parent
terminal, both supervisors, each epoch verification, source hashes, and exact
agreement of saved/reference selections. Every copied immutable receipt,
seed and landscape file is hash-bound at both source and destination. The
old directory remains sealed. Only the new directory's truncated-epoch cloud
audits and stage record are removed so that the extended cloud can be checked.

The cumulative cap is200, comprising100 inherited receipts and at most100
new invocations. The unchanged runner replays the old receipts, resumes with
the next map, and freshly audits and replays the combined point cloud. Cached
reference landscapes inherit their previous independent CVP verification;
CVP is not recomputed for an unchanged landscape. A gain still rebuilds
immediately, and any new landscape receives full rational-CVP replay.

The bounded run is under
`artifacts/local/elliptic-curves/curve92-pairwise-cached-v3-discovery-v1/`,
with separate1800-second/3-GiB search and replay limits, one worker and the
same height125000 /10-second chart bounds. This extension completes100 new
invocations,200 cumulative, with no timeout or gain: M26→M26. Search takes
84.431 seconds, peak RSS322752512 bytes. Replay passes in3.599 seconds,
peak271437824 bytes, reusing the bound prior full-landscape CVP verification.
The [sealed continuation result](../../artifacts/generated-results/elliptic-curves/curve92_pairwise_cached_v3_v1/result.json)
reports200 cumulative charts; the protocol separately identifies100 inherited.
These are not200 new point-search invocations.

A second cached continuation, under `curve92-pairwise-cached-v3-discovery-v2/`,
has a cumulative cap of300 and the same supervisor limits. With no gain,
only92 invocations remain before this146-centre/two-map policy is exhausted.
The final suffix completes92 new invocations,292 cumulative, with no timeout
or gain: M26→M26. Its terminal reason is
`FINITE_POLICY_EXHAUSTED_NO_CERTIFIED_GAIN`. Search takes67.260 seconds,
peak RSS329203712 bytes; replay passes in4.068 seconds, peak280129536 bytes.
The [final sealed result](../../artifacts/generated-results/elliptic-curves/curve92_pairwise_cached_v3_v2/result.json)
closes this selected146-centre/two-map policy. The original queue's
`queue-resolution.json` binds the completed descendant so this suffix is not
scheduled again. No rank27 point, conductor improvement or rank upper bound
follows from this completed bounded miss.

## Preconditioned coordinate preflight

Before another point search, a fixed audit constructs the lean preconditioned
map at the first16 centres of the completed pairwise policy. All16 reproduce
already completed height125000 projective boxes, allowing the exact
height-preserving substitutions `t`, `-t`, `1/t`, `-1/t`. Infinity must be
included for this comparison; the parent receipts explicitly check it.

The [exposure audit](../cas/audit_preconditioned_box_exposure.py) performs no
point search. A [separate verifier](../cas/verify_preconditioned_box_exposure.py)
checks the parent terminal/selection/receipt-chain bindings, replays all292
old chart witnesses, checks all16 new exact map identities, and reproduces
the16 duplicate-box decisions. Three regressions check signed permutations,
projective scaling, finite height boxes including zero/infinity, rejection of
singular matrices, and non-equivalence of a translated box.

Evidence: `artifacts/local/elliptic-curves/curve92-preconditioned-exposure-v1/`,
including `report.json` and `exposure-verified.json`. These16 proposed repeat
searches are skipped. The result concerns the fixed16-centre roster only;
it does not exclude other coordinates or a rank27 point.

## Outside-span parent branch

The [302-motivated complement strategy](PRODUCTIVE_PARENT_SPAN_REASSESSMENT_2026-09-09.md)
now supplies15 new generic classes outside the3-dimensional span of curve92's
productive parents. Together the old and new masks span all16 generic parity
dimensions. This is not coverage of all parity classes or a new rank claim.

All128 deterministic sampled generic minima are checked by both exact solvers.
Their doubled-Gram norm counts are7:1,11:17,12:24,15:24,16:47,19:15.
The15 classes at the largest sampled norm19 (generic norm9.5) form the new
bank. Preparation takes2.045 seconds under120 seconds/1GiB. The resulting708
centres are all new up to sign relative to the302 distinct centres in the
completed productive and pairwise clouds. Preflight takes77.400 seconds under
300 seconds/1GiB. No point search occurs during preparation or preflight.

The first100 actual point invocations complete at50 centres: M26→M26,
zero map timeouts, zero point timeouts and zero duplicate-box skips.
Search takes92.466 seconds, peak process-tree RSS158937088 bytes. Independent
replay passes in247.369 seconds, peak293699584 bytes, including the full
original rational-CVP landscape, exact maps, point witnesses, receipt chain
and whole-cloud mod2/3/5 checks. All three lower bounds remain26.

- [Sealed complement pass](../../artifacts/generated-results/elliptic-curves/curve92_complement_v3_v1/result.json).
- Preparation: `artifacts/local/elliptic-curves/curve92-complement-preparation-v1/`.
- Search: `artifacts/local/elliptic-curves/curve92-complement-v3-discovery-v1/`.
- Cursor: centre50, `preconditioned_full`, with658 centres unvisited.

A cached continuation was prepared in
`artifacts/local/elliptic-curves/curve92-complement-cached-v3-discovery-v1/`.
Its preparation passes in0.487 seconds under60 seconds/1GiB and binds the
original proof and receipts. Its budget is100 additional invocations,200
cumulative; the completed result is below. Existing completed
productive and pairwise policies remain closed.

The new branch's budget is exhausted, not its selected policy. No rank27
direction, conductor improvement or global upper bound is obtained.

### Completed outside-span continuation

The next100 actual point invocations complete at centres50–99,200 cumulative:
M26→M26, zero map or point timeouts. Search takes100.828 seconds, peak RSS
158871552 bytes. Replay passes in3.052 seconds, peak73138176 bytes, inheriting
the bound full CVP proof while rechecking maps, all200 point receipts and the
extended whole cloud. Mod2, mod3 and mod5 lower bounds remain26.

[Sealed continuation](../../artifacts/generated-results/elliptic-curves/curve92_complement_cached_v3_v1/result.json).
Its `continuation-queue.json` retains centre100, `preconditioned_full`, with608
centres unvisited. The first complement pass's `continuation-head.json` points
to this verified descendant. This is budget exhaustion, not policy exhaustion
or a rank upper bound. No rank27 point or conductor improvement is obtained.

The second cached continuation adds100 invocations at centres100–149,
300 cumulative in this branch. M26→M26, zero map or point timeouts; mod2/3/5
lower bounds remain26. Search102.124 seconds, peak RSS159768576 bytes, and
replay4.076 seconds, peak82948096 bytes, both pass. The full CVP proof remains
inherited from the independently verified first pass; all300 point receipts
and the extended cloud are checked again.

[Sealed second complement continuation](../../artifacts/generated-results/elliptic-curves/curve92_complement_cached_v3_v2/result.json).
Raw evidence is under `curve92-complement-cached-v3-discovery-v2/` in the local
elliptic-curve artifacts. Its cursor starts at centre150, `preconditioned_full`,
with558 centres unvisited; the preceding continuation's head points here.
The branch remains budget-truncated, with no new rank or conductor claim.

## Third cached complementary-parent continuation

The next bounded pass completes100 additional point calls,400 cumulative
on the complementary-parent branch, without a new certified direction or
point/map timeout. Search takes107.300seconds; independent cached replay
passes in5.167seconds. The lower bound remains26, so the potential rank27
conductor improvement remains unachieved. The exact next cursor is centre200,
`preconditioned_full`, leaving508 of708 centres unvisited. No rank upper bound
follows from the bounded miss.

- [Replayed third cached pass](../../artifacts/generated-results/elliptic-curves/curve92_complement_cached_v3_v3/result.json).
- [Current continuation cursor](../../artifacts/generated-results/elliptic-curves/curve92_complement_cached_v3_v3/suffix-queue.json).
