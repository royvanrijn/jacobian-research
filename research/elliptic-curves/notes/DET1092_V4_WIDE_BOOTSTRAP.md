# V4 wide-bootstrap historical panel

This terminal, bounded panel has no current claim in
[MATH_STATUS.json](../../MATH_STATUS.json). Its local controller records
`COMPLETE_V4_ROSTER`; it is not a request to launch, resume, or expand V4.

The repaired protocol generated the complete nonzero mod-2 quotient directly,
using the 104,978-row norm-8/10/12 TSV only as a cross-check rather than as a
complete mask table. This corrected the preflight assumption that caused the
first empty launch. It then exposed 512 previously untouched parity classes on
each of eight frozen determinant-1092 fibres: 4,096 fresh exact-CVP charts in
total. The local report records eight independently verified lower-bound-17
outcomes and no certified gain. That finite null is neither an exact-rank result
nor a proof that no jump or alternative chart exists.

The full frozen protocol, controller commands, inputs, and replay description
are preserved byte-for-byte in the
[elliptic archive](../../archive/elliptic-curves/notes/DET1092_V4_WIDE_BOOTSTRAP.md.txt).
The regression driver remains
[`run_det1092_v4_bootstrap.py`](../cas/run_det1092_v4_bootstrap.py). Any new
visibility experiment needs a new mathematical gate, frozen population, and
separate budget; it must not continue this panel automatically.
