# Exact incidence of all185 inventory curves

The84 additions to V13 have complete rational j-incidence certificates against
the same twelve recorded family presentations:1008 pairs, with920 exclusions
and88 complete preimage lists. No pair remains unresolved. The audit takes
2.023523 seconds and its independent Sage-free replay0.436152 seconds.

Combined with the prior101 curves, the [exact aggregate](../../artifacts/generated-results/elliptic-curves/inventory185_incidence_v1.json)
accounts for all2220 pairs:2010 exclusions and210 complete preimage lists.
All185 own-family parameters are recovered. The25 additional presentations
are the already proved duplicate pair08234 and published R17.

The four new duplicates are IDs111,152,173,174. Their base parameters satisfy
compact = -26 published -50. The retained exact Q(t) model identity and
unimodular17-section matrix prove the same integral generic subgroup under
this map. They supply no new independent generic direction or new curve.
Equal j alone would not justify that conclusion; the separate model/section
transport is essential.

The finite audit uses full projective images at good map reductions through251.
Where these do not exclude a target, it retains exact rational factors and
modular no-root witnesses for every residual factor, with prime bound997.
Infinity is explicitly checked. The independent checker verifies the rational
j-map identities, unchanged projective degree, every image and factorization,
and the complete target/presentation product.

The sources are `run_inventory185_added_incidence.py`,
`audit_inventory185_added_incidence.sage`, `replay_inventory185_added_incidence.py`
and `certify_inventory185_incidence.py` under `../cas/`. The fixed audit has
1200 seconds, its replay600 seconds and one2-GiB worker, without retries or
budget extensions. These are geometry certificates, not a parameter or point
search. They exclude no unlisted family, nongeneric point or higher rank.

## Portable replay

The [standalone replay](../../artifacts/generated-results/elliptic-curves/inventory185_incidence_portable_replay_v1.json) passes all three stages: every one of the2220 exact incidence witnesses, the aggregate, and the generic model/section transport. The [3531394-byte archive](../../artifacts/generated-results/elliptic-curves/inventory185_incidence_evidence_v1.zip) requires no base archive and retains the note as frozen before this completion update. Rank proofs are outside this geometry supplement.
