# Fresh outer MW16 parameters with deeper retention

**The fixed scan, all scalar scores, all 2,580 point boxes and local exact
proofs pass. Four fibres have certified lower bounds 26, 24, 24 and 23,
unmatched in the pinned catalogue and prior-equation snapshot. All 182 isolated
point-proof stages and the 195-curve inventory/CSV replay pass.**

The [completed three-band trial](MW16_OUTER_PARAMETER_BANDS_2026-09-06.md)
found two rank-at-least23 curves in4096<H<=16384. One stood504th of512 in
its signed short-score list, and1016th of1024 in its merged family pool.
A smaller short prefix would have discarded it. This is one exact retention
observation, not a density or sensitivity estimate.

The new experiment gives each of the five MW16 families and each sign one
unused denominator residue modulo16 in the same outer annulus. SHA256 chooses
among the seven remaining residues of the same parity as its earlier slice.
All **95619502 primitive addresses** are disjoint from the previous three-band
cohort and from the compact4096 square. Previously selected fibres are not
used as centres or neighbourhood targets. Parameter novelty alone does not
prove equation novelty.

Retention rises from512 to4096 per signed slice, preserving40960 candidates
before longer-prime scoring. All ten complete small-frame/top-seven tests,
the first full-slice runtime gate, canonical562-prime score checks and
independent primitive counts pass. No scan or retained-score replay remains. The separate exact homogeneous-model
audit and replay also pass: all 40,960 models have distinct j-invariants.
This is within-cohort distinctness, not a claim of catalogue absence.

The separately frozen scalar stage covers4099..65521, with two independent
character sums per candidate. The first ten rows, two per family, pass the
runtime gate, projecting5125.214 seconds serial against8000 allowed. Four
workers,20 seconds per call, a3600-second outer cap and80-row checkpoints
apply. Complete score replay has a600-second cap. Then select twelve distinct
curves per family,60 total, by combined S1 through65521, good count,
denominator and signed numerator. Disjoint65537..131071 validation cannot
alter selection. Known-record equations, parameters, points, ranks and
jump labels remain outside candidate selection and execution.

The separately frozen point scope is60 generic16-only fibres,43 maps
each, height125000 and ten seconds per chart, all maps before points. No
adaptive follow-up or automatic refill is included. Failures and censoring
are retained without retry. This is a search on fresh parameters with deeper
retention, not complete annulus coverage, a rank predictor or a rank bound.

Sources: `../cas/scan_mw16_fresh_retention.py`,
`../cas/finish_mw16_fresh_retention_scan.py`,
`../cas/score_mw16_fresh_retention.py` and
`../cas/finish_mw16_fresh_retention_scores.py`.
Local protocols and logs are under `mw16-fresh-outer-retention-v1` and
`mw16-fresh-retention-scores-v1` in `artifacts/local/elliptic-curves`.

The point, terminal certification and isolated replay controllers are frozen
and wait for successful upstream proofs. Sources have prefix `fresh60_mw16`
and use the local `fresh60-mw16-pari-v1` directory. The point-only portable
verifier binds that exact directory, incorporating the path correction from
the previous outer60 replay.

## Certified new fibres

| Family | Parameter | Certified rank lower bound |
|---|---:|---:|
| a1-fibration-01 | -4444/217 | 26 |
| a1-fibration-04 | -6577/1092 | 24 |
| a1-fibration-04 | 4584/115 | 24 |
| a1-fibration-04 | 4104/563 | 23 |

All 40,960 extended scores and 81,920 direct character-sum checks pass.
Forty of the sixty finalists lie beyond the signed short-score prefix of
512 in their respective fresh slices. This directly measures the reach of
retention, without asserting that the earlier experiment would have found
these fibres.

All 60 maps precede all 2,580 completed boxes. Exact point admissions,
rational chart maps, generic transports and full-cloud calculations modulo
2, 3 and 5 pass. The final bounds across all sixty fibres are 36 at 16,
8 at 17, 7 at 18, 3 at 19, and one each at 20, 21, 23 and 26, plus two at 24.
All sixty are unmatched among 977 previously measured equations and the
pinned 593-equation catalogue. Equations and independent point certificates
are in `fresh60_mw16_results_v1.json`; `fresh60_mw16_experiment_v1.json`
records the complete trial. These are lower bounds, not exact ranks or
proofs of universal novelty. The cohort supplies no new rank-at-least-28 curve.


The additions are inventory IDs 192 (26), 193 (24), 194 (24), and 195 (23).
Their signed short-score positions are respectively 528, 416, 1567, and 1397;
three would be excluded by keeping only 512 in these same fresh slices.
The point-only portable archive is 61,111,597 bytes and passes all 182 isolated
stages. Inventory V17 preserves every old ID and independently replays all
195 point certificates, mutual distinctness, catalogue comparisons and CSV.

The four globally minimal equations have largest coefficients of 241, 245,
245 and 218 bits respectively. Exact invariant valuation arguments exclude
scaling at every prime; the respective c4/c6 gcds are 1, 64, 169 and 13225.
All rational point transports and finite independence proofs replay.
The [executable four-curve Sage export](../../artifacts/generated-results/elliptic-curves/new_fresh60_high_rank_curves.sage)
constructs each minimal equation and every certified point; direct Sage
execution passes. The proof is `fresh60_high_rank_minimal_proof_v1.json`.
