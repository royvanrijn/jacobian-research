# Frozen class-1 arithmetic intake

**Operational update:** the subsequent user authorization starts
[ordinary prospective search](CLASS1_PROSPECTIVE_ORDINARY_SEARCH_2026-09-10.md).
The strict-class prerequisite below is historical for ordinary search and
still applies only to that separate construction intake. Parent bytes remain
frozen; the unavailable strict checker does not block ordinary V3 exposure.

The [certified class-1 parent](X1092_CLASS1_RATIONAL_MW17_REALIZATION_2026-09-10.md)
is frozen byte-for-byte, SHA256
`7c6ee40c46f5a1f3d1fc464b5685a0e4c77ed1f3347b67865d7c9f93b2acd862`.
**Class 1 only; no further parent construction, parameter panel or V3 release.**

The immutable package is
[`x1092_class1_arithmetic_gate_v1`](../../artifacts/generated-results/elliptic-curves/x1092_class1_arithmetic_gate_v1/freeze.json).
It copies the exact parent, realization manifest, marking and section
certificates. The freezer verifies every existing realization proof/software
hash and refuses to replace an existing file with different bytes. The
original certified files and source checker have not been changed.

## Prepared inputs

[`generic-arithmetic.json`](../../artifacts/generated-results/elliptic-curves/x1092_class1_arithmetic_gate_v1/generic-arithmetic.json)
contains only the generic arithmetic data:

- The marked separable cubic algebra `Q(s)[theta]/(theta^3+A(s)theta+B(s))`.
- Its exact coefficients and discriminant, with basis `1,theta,theta^2`.
- The seventeen inherited section representatives `x_i-theta`, their exact
  norm identities `N(x_i-theta)=y_i^2`, multiplication matrices and bindings
  to the certified height Gram and source frame coordinates.

These norm identities were checked by resultant and multiplication-matrix
determinant, then replayed separately from the sealed packet. They do not
compute the complete inherited everywhere-even half-ideal image. That image,
the strict and ordinary-unramified character spaces, maximal order and bad
support remain uncomputed. No parameter or prospective class was selected.

## Novelty checker is unavailable

Agent 1 explicitly clarified that no applicable prospective novelty checker
currently exists, nor has one been proved forthcoming. Its developing
fixed-number-field principal-recipe adapter will not itself certify novelty
and does not automatically apply over `Q(s)`. No such adapter was imported or
presented as a novelty gate here.

[`intake-status.json`](../../artifacts/generated-results/elliptic-curves/x1092_class1_arithmetic_gate_v1/intake-status.json)
therefore records **UNAVAILABLE_APPLICABLE_PROSPECTIVE_NOVELTY_CHECKER**. The
intake ignores claimed PASS/rank/independence labels as mathematical evidence
and has no launch function. Five regressions check closed gates, forged
positive statuses, the fixed parent hash, immutable writes and copy tampering.

When Agent 1 supplies an actually implemented checker, integrate and pin its
source, CLI/schema, mathematical domain and proof dependencies in a new
version of the intake. Its input must bind this exact marked cubic and the
complete inherited image; it must replay the required principal-square and
novelty certificates prospectively. No placeholder verifier can pass.

The required order remains:

1. Applicable exact prospective class-novelty certificate.
2. Bounded cover construction for those certified classes.
3. Certified rational solubility and point recovery.
4. Exact independent quotient-direction certificate on the identified fibre.
5. Only then permit V3 for the certified direction.

Other classes and the parameter panel remain disabled. A successful recipe,
carrier identity, local-survival result, soluble cover or point list alone
cannot release V3. The current unavailable interface is an implementation/
theory gap, not a mathematical exclusion of strict classes on class 1.

## Replay

```sh
python3 research/elliptic-curves/cas/freeze_x1092_class1_arithmetic.py --check
sage -python research/elliptic-curves/cas/verify_x1092_class1_generic_arithmetic.sage
python3 research/elliptic-curves/cas/x1092_class1_novelty_intake.py
python3 -m unittest discover -s research/elliptic-curves/cas -p test_x1092_class1_novelty_intake.py -v
```

The generic producer was run under a 120-second cap and completed in less
than one second including Sage startup. No number-field class-group campaign,
new carrier construction, cover solving or specialized point search was run
after the freeze instruction.
