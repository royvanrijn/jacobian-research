# Curve74: native M26 short-pass continuation

The R17 family07ca9 fibre at `613/828`, inventory74, receives a frozen
100-total-invocation first pass. Its existing M26 comes from
`retention24_r17_results_v1.json`, entry `07ca9-280`. The native adapter
rechecks the generic17, exact model transport, full26-point certificate and
retained gain history. Four productive generic masks are sealed:
39666,47676,53235,54404. Their generic CVPs independently replay. Preparation
completes in8.081 seconds under120 seconds/1GiB, with no point search.

The scheduling motivation is a different native family with a high certified
seed and a two-direction conductor payoff in the pinned comparison: a new
rank-at-least28 certificate would warrant a fresh catalogue/conductor check.
This is not evidence that two additional directions exist or a current record
claim. Only points independent of the supplied26 count as gains. No public
higher point or retrospective visibility oracle enters selection.

The [short-pass runner](../cas/run_short_seed_v3.py) preserves V3 selection,
both maps, height125000,10 seconds per invocation, immediate rebuild on a
standalone certified gain, and100 total invocations across all epochs.
Search and independent replay each have1800 seconds/3GiB and one worker.
Preparation and receipts are under
`artifacts/local/elliptic-curves/curve74-v3-preparation-v1/` and
`curve74-short-v3-discovery-v1/`, with sibling supervisors and frozen sources.
The first pass completes100 invocations with no timeouts or certified gain:
M26→M26. Independent replay passes. Search takes89.621 seconds, peak RSS
325718016 bytes; replay takes56.290 seconds, peak301105152 bytes. Both maps,
the full selected landscape, exact point witnesses and complete-cloud finite
certificates are replayed. The terminal reason is `CHART_BUDGET_EXHAUSTED`;
the untested suffix remains UNKNOWN.

[The sealed result](../../artifacts/generated-results/elliptic-curves/curve74_short_v3_v1/result.json)
retains the seed and completion records. `continuation-queue.json` preserves
the next map/centre for a possible cached continuation. No new lower bound,
rank28 direction or conductor improvement was found by this prefix.
