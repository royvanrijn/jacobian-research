# Blind factor-free recovery and prospective point exposure

The full-roster factor-free regression passes: starting only from our original
27-point subgroup on inventory curve188, it recovers an independent 28th
direction. All49 declared boxes complete. This reproduces a known public28
curve; it is not a new curve or a new record.

The prospective follow-up completes all539 boxes on eleven retained fibres
across three parent choices. Independent proofs certify all8,907 retained
point witnesses and **no added direction**. No new parameter population is
generated. A separate98-box pilot on two fixed fibres of the newly reconstructed
determinant1092 MW17 parent also completes, with both certified bounds
remaining17. In total,637 prospective boxes yield no added direction.

## What the regression closes

The earlier [exceptional-direction recovery](INVENTORY188_EXCEPTIONAL_DIRECTION_RECOVERY_2026-09-07.md)
used the old coordinate pipeline on all49 charts. The later factor-free control
used one retrospectively chosen successful chart. Neither alone established the
requested full-roster factor-free regression.

`blind_factor_free_28_control.py` now regenerates all2048 deterministic parity
masks and all49 selected charts from the original27 seed. It imports neither
old maps nor the successful chart index. The sampling domain is the original
`full11952-specialized-followup-v1` domain. A384-bit numerical canonical-height
matrix, rounded at10^6, supplies a unimodular LLL/CVP metric. Exact arithmetic
checks every parity and norm transport. The49 largest computed norms determine
the roster before any point search; CVP optimality is not asserted.

The mapper uses the integral Gauss quartic, verified square-content scaling and
`hyperellred`, without the earlier global-minimal-quartic preparation. Every
box has height125000 and a ten-second limit. All49 run regardless of recovered
rank. Centres remain in the original27 subgroup while point admissions can
extend the search state.

A Python audit hook installed before research-module imports rejects artifact
opens outside this run directory and the original27 seed. The retained logs
pass this whitelist check. This is a process-level audit, not an OS sandbox.
Prior knowledge that one historical chart succeeded is explicitly disclosed;
it supplies no point, chart index or selection input to this run.

The fifth chart recovers the extra direction. Exact chart replay proves that
the returned rational point came from the recorded quartic square witness.
The original27-point basis remains the prefix of the28-point final basis.
All49 retained point-cloud witnesses give finite quotient ranks28 modulo2,3,5.
A separate Sage process, using only a copied verifier and point certificate,
enumerates the finite groups and independently certifies28 directions.
No unconditional rank upper bound is claimed. The nine supervised geometry,
search and certification stages consume67.706518982 seconds in total.

The compact [control report](../../artifacts/generated-results/elliptic-curves/blind_factor_free_28_control_v1.json)
binds the original input, access logs, protocol, transcripts and standalone
certificates. Check it with:

```sh
python3 elliptic-curves/cas/report_blind_factor_free_28.py --check
python3 elliptic-curves/cas/audit_recorded_point_mod2_rank_v3.py --check artifacts/generated-results/elliptic-curves/blind_factor_free_28_control_mod2_v1.json
python3 elliptic-curves/cas/audit_retained_cloud_modl.py --check artifacts/generated-results/elliptic-curves/blind_factor_free_28_control_modl_v1.json
```

## Completed prospective cohort

The run is `artifacts/local/elliptic-curves/prospective-factor-free-portfolio-v2`.
Its controller is `prospective_factor_free_portfolio_v2.py`.

- All seven unmatched inventory curves with certified27 directions: IDs186,
  40,41,48,71,72,90. Their343 factor-free maps were already proved to expose
  new finite-coordinate boxes and had not been point-searched.
- IDs42 and49, the two strongest remaining rank26 source gaps from different
  fibration labels in the existing frozen training-score order. The previously
  completed pair74/50 is excluded by exposure history. No validation scores
  enter selection.
- Mestre u=11,T=1, now starting from its certified12-point subgroup. The tie
  with u=13 is broken by smaller u.
- Positive Kihara v=3,T=1, now starting from its certified14-point subgroup.

The seven rank27 curves and the two rank26 curves all lie on X948; their
fibration labels are not separate parents. The Mestre and Kihara fibres add two
Q-distinct parent surfaces. Selection uses only retained equations and prior
certified subgroups. ID90 inherits16 MW16 sections; the other X948 rows
inherit17. The protocol row values and exact mask checks bind this distinction;
its shorthand centre-policy sentence saying generic17 is not universal. The new-parent masks require a nonzero coefficient beyond
the earlier11/12-point subgroup, respectively. The `generic_points` transport
field there means the inherited specialized subgroup, not a new generic-rank
claim.

Every curve receives49 fixed charts at height125000, ten seconds each. All539
maps must freeze before the first point search. There is one worker, a2GiB
memory bound,180 seconds per geometry stage,600 per point stage and300 per
history replay. There is no rank stopping, outcome-based refill, new parameter
scan or automatic following wave. Whole-cloud and independent rank checks
follow completed exposure; scores alone cannot count as success.

Version1 is preserved: preparation rejected a deterministic-domain mismatch
on the second retained27 roster, before any point box ran. Six historical
rank27 rosters use `new27-specialized-parity-v1`, whereas ID186 and the new
rosters use `full11952-specialized-followup-v1`. Version2 explicitly binds each
reused roster's original domain, with no changed curve selection or searched
outcome. Failed preparation time remains part of the campaign cost.


## Prospective outcome and cost

| Prior subgroup | Curves | Completed boxes | Added certified directions |
|---|---:|---:|---:|
| Retained27 | 7 | 343 | 0 |
| Retained26 | 2 | 98 | 0 |
| Mestre12 | 1 | 49 | 0 |
| Positive Kihara14 | 1 | 49 | 0 |

Every row passes exact2048-mask transport, all49 rational maps and raw-square
point provenance, adaptive-history replay, complete-cloud finite quotients
modulo2,3,5, and standalone Sage group enumeration. The retained point counts
are99,89,77,33,71,63,45,78,298,987,7067 in protocol order. These finite quotient
ranks certify lower bounds; they do not prove that every returned point lies
in the prior rational span or that further independent points do not exist.

The [portfolio report](../../artifacts/generated-results/elliptic-curves/prospective_factor_free_portfolio_v2.json)
records1,027.571582408 supervised seconds, including3.352286877 seconds of
failed version1 preparation and both freeze stages. Point-search CPU time,
worker wall time, geometry and verification costs remain separate per curve;
nested driver time is not counted twice. All66 standalone certification
stages and22 worker/history stages pass. Check the result with:

```sh
python3 elliptic-curves/cas/report_factor_free_portfolio.py --check
```

The control establishes recoverability on one known curve. The prospective
null result does not establish sensitivity on all retained fibres or support
another automatic population increase.

## New parallel parent: separate follow-up

The newly completed [explicit MW17 parent](CURVE302_RECOVERED_MW17_PARENT_2026-09-07.md)
has arithmetic determinant1092 and differs from production948. This is a new
construction input, not a refill selected from the eleven-curve outcome.
The separate `det1092-point-pilot-v2` protocol chooses exactly T=1 and1009/101,
with no scores and with the known anchor T=0 excluded. A dedicated input
exports only the generic equation and17 rational sections; public rank31
embedding words, anchor parameter and exceptional points are absent.

Both fixed fibres have17 exactly independent specialized sections. Since they
have no additional own subgroup yet, their2048-mask sample includes all
nonzero classes of the17-point group (`mask_floor=0`). The generic dimension
is still17. Each receives49 factor-free charts at the same height125000 and
ten-second box limit, all maps frozen before points. Independent exact
specialization and coordinate transports precede searching.

The first intake version compared stored string coordinates directly with
Fraction coordinates. Its rank check itself succeeded at17 on both fibres;
the representation equality failed. Version2 compares exact rational values.
No parameter, section, rank gate or search policy changes. The failed intake
and finite diagnosis remain retained and charged. No point box ran in version1.


The [new-parent pilot report](../../artifacts/generated-results/elliptic-curves/det1092_point_pilot_v2.json)
now closes all98 boxes. T=1 and1009/101 have1196- and1318-bit displayed short
coefficients respectively. Their49 maps take1.970 and2.072 seconds to prepare.
Every point box completes, but **no finite point is returned**: each complete
cloud consists of its17 certified input sections. The34 input points pass
exact independent specialization, coordinate transport and full finite-group
proofs modulo2,3,5. Both equations are unmatched in the pinned620 catalogue
and201 inventory; this is not literature-wide novelty or a near-record claim.

The pilot consumes130.110901956 supervised seconds, including0.890578791
seconds for the failed intake and its finite diagnosis. Point-search CPU time
is31.093 and33.074 seconds; geometry, worker wall and proof costs are separately
recorded. All12 standalone cloud/geometry/rank stages pass after the exact
seed transport replay. Check the completed report with:

```sh
python3 elliptic-curves/cas/report_det1092_point_pilot_v2.py --check
```

The full-roster blind control passed, but neither prospective cohort added a
direction. In particular, fast map preparation does not establish adequate
point visibility on the larger new-parent fibres. These completed trials do
not justify a new parameter-population sweep or a rank upper bound.

The subsequent [restricted-prime refinement and invariant height proof](BOUNDED_PRIME_EXPOSURE_AND_HEIGHT_FLOOR_2026-09-07.md) preserves these results, avoids98 repeated boxes and completes98 additional boxes on the same two new-parent fibres.
