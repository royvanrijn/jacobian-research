# New 11952 fibres beyond the complete height-131072 square

**Completed bounded experiment. All 64 new fibres have exactly certified
17-point subgroups; the 3,136 completed point boxes found no stronger bound.
All 194 isolated point-proof stages pass. This is not an upper bound on rank.**

The two completed 11952 cohorts each supplied a new curve with 27 certified
independent points. Their parameters lie inside the square whose 20.89 billion
primitive short-score addresses have already been scanned. This experiment
uses a disjoint annulus:

\[
131072 < H=\max(|n|,d) \le 524288,\quad t=n/d,\quad \gcd(n,d)=1,\ d>0.
\]

Four denominator slices per sign are frozen modulo 16384, one SHA256 choice
in each quarter of the residue interval, with balanced parity. Their
**76,709,238 primitive addresses** are all outside the former complete square.
They are not neighbourhoods of previously successful parameters. These slices
are not complete annulus coverage, and parameter novelty is not equation novelty.

All ten new signed/reference frame regressions pass, including complete small
populations and top-seven ordering. The first full slice passes its 25-second
runtime gate in 2.58 seconds; all eight scanner calls take between 2.58 and 2.84
seconds each. Every one of the 4,096 retained candidates per slice passes
independent canonical 562-prime score replay and primitive population checks.

## A compact-height restriction found in the scorer

The v1 cached-score reader rejected the frozen new bank with `invalid primitive
candidate` before producing any scores. Its input validator hardcodes the old
131072 bound. This was a software domain restriction, not an arithmetic or
rank exclusion. The failed protocol, candidate bank and raw response remain
under `11952-new-annulus-scores-v1`; the prepared v1 point controller was never
launched.

The separate v3 cache reader accepts an explicit maximum height, retaining
131072 as the default and rejecting bounds above 2^30 for integer safety.
This experiment explicitly passes 524288. Quantized table lookup, signed
residues, projective infinity and summation are unchanged. Its gate checks
three boundary/framing tests, exact agreement on all 967 recorded old scores,
and ten fixed new boundary parameters against fresh scalar PARI traces and
20 independent character sums. The gate and its independent replay pass. The replacement v2 score
controller completes all ten stages, using the same frozen parameter bank.
No parameter scan is repeated.

## Fixed exposure and completed proofs

The v2 score protocol applies the previously certified projective cache
through 32749 to all 32,768 survivors. It then freezes 4,096 distinct equations
by combined S1 through 32749, using 977 prior equations and the pinned 593
catalogue equations only as exclusions. Fresh scalar traces through 65521
must agree with the cached first extension and pass two direct character sums
per curve. A first-eight runtime gate, two workers, 20 seconds per call and
16-row checkpoints apply.

Only after all scalar scores replay are 64 finalists frozen. The independent
selection replay checks the exact exclusions and ordering. Validation at
65537 through 131071 is obtained afterward and cannot alter selection.
The separately prepared point scope is 64 generic-17 fibres, 49 charts each,
height 125000 and ten seconds per chart, with all maps before all points.
There is no adaptive refill or follow-up wave. New point subgroups require
exact independence certificates; scores do not imply ranks.

Sources: `../cas/scan_11952_new_annulus.py`,
`../cas/finish_11952_new_annulus_scan.py`,
`../cas/benchmark_11952_annulus_cache_v3.py`,
`../cas/score_11952_new_annulus_v2.py`, and
`../cas/finish_11952_new_annulus_scores_v2.py`.
Local evidence is under `11952-new-annulus-v1`, `11952-annulus-cache-v3`,
`11952-new-annulus-scores-v1` and `11952-new-annulus-scores-v2` in
`artifacts/local/elliptic-curves`.


## Visibility diagnostic on the new fibres

A separately frozen diagnostic takes the original scalar-selection indices
0, 16, 32 and 48, without reading point outcomes to choose them. These four
addresses are all in the new annulus. Their original map files are complete
before the diagnostic is frozen. Each independently certified generic-17
basis is split into a 16-point input and a withheld-section oracle.

Twelve centres per fibre are chosen from 256 fixed sampled parity masks using
only the retained metric block. All 48 boxes use the original height 125000
and ten-second chart budget. Oracles are opened only after all four masked
searches and geometry replays complete. A recovered known direction requires
an exact rational group relation with a nonzero withheld coefficient;
unresolved relations remain UNKNOWN. This does not calibrate exceptional
rank-jump sensitivity, supply new ranks or alter the original 64-fibre batch.

The first diagnostic stopped before point search because its guard compared
serialized coordinates with parsed rational coordinates. The preserved v1
failure is a representation mismatch, not a failed independence proof. The
separate v2 sources parse the coordinates exactly and retain the original
four fibres and budgets.

All 48 boxes complete, and all four fibres recover their withheld direction.
The independent Python checker verifies the generic independence certificates,
raw witness provenance and exact group relations after clearing denominators.
Its certificate is `annulus11952_masked_recovery_replay_v1.json`; the source is
`../cas/verify_annulus11952_masked_recovery.py`. The corrected controller is
`../cas/finish_annulus11952_masked_controls_v2.py`, with checkpoints under
`annulus11952-masked-controls-v2`. Known-direction recovery is not evidence
that every exceptional direction would have been visible.

The original 64-fibre proof is recorded in `annulus64_r17_results_v1.json`
and `annulus64_experiment_v1.json`. All 64 are unmatched against the frozen
977 prior equations and 593 catalogue equations. Their generic transports,
rational chart geometry and full point clouds modulo 2, 3 and 5 replay.
`annulus64_point_portable_replay_v1.json` records 194 successful isolated
checks; the point-only archive does not rerun the parameter or score searches.

The subsequent [full-score fresh-slice trial](JOINT11952_FRESH_ANNULUS_2026-09-06.md)
addresses a measured loss from truncating short scores before longer scoring.
