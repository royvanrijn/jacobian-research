# Completed foundry-v3 conductor snapshot

The stopped foundry-v3 conductor queue completed every one of its 525 sealed
endpoints. For the fixed 94-curve V2 publication cohort, the completed results
provide 71 exact conductors and 23 partial `UNKNOWN` bounds. This supersedes the
earlier eight-result cutoff for the same curve cohort.

The [completed snapshot](../../artifacts/generated-results/elliptic-curves/foundry_v3_conductor_snapshot_v2.json)
and its fresh replay of all 94 certificates are the canonical sources. Exact
status requires a complete remaining cofactor of one; partial factorizations
remain explicitly `UNKNOWN`. This adds 65 exact conductors to the rendered
curve inventory. No exact-rank, conductor-record, or worldwide-novelty claim
follows.
