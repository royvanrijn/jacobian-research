# Bounded commissioning across nonisometric X1092 frames

The user has replaced indefinite class1 parameter expansion with a fixed
commissioning endpoint: **64 score-selected fibres and16 independent controls**.
The active65,536-address score table, scoring formula, point budgets, generic
parent and control hash order are unchanged. At amendment,58 ranked fibres
and8 controls had been dispatched. The original controller drained its two
active batches and exited; the capped controller resumes their retained packets.

Selection takes the first64 rows from the existing ranked stream and the
first16 from the existing control stream. It retains their within-arm order.
After the ranked quota fills, remaining controls complete. This changes exposure
quotas, not the score or its cutoff retrospectively. The entire score distribution,
selected rows, exact terminal packets, full-cloud references and computation
costs are retained as the matched baseline. Earlier v1 commissioning remains
separate because its control stride was sign-confounded.

## Branching rule

- A certified lower bound≥20 keeps its existing adaptive escalation, including
  full-cloud reconciliation and independent finite replay. The frame transition
  remains closed after such a result, pending assessment of the escalated fibre.
- If all80 endpoints complete and none reaches≥20, the operational classification
  is `NO_EVIDENCE_CURRENT_SEARCH_PRODUCTIVE`. This is no rank statement, no
  upper bound on any fibre, and no theorem excluding this parent.
- Censored or failed endpoints without a gain produce
  `UNKNOWN_INCOMPLETE_COMMISSIONING`; incomplete arithmetic cannot release a
  purported complete null panel.

The code is [run_frame_commissioning.py](../cas/run_frame_commissioning.py).
Its immutable amendment binds the original plan, score table and queue. The
frozen ordinary worker performs every search; no model calls occur.

## Next frame and realization gate

The [retained prospective geometric ranking](../../artifacts/generated-results/elliptic-curves/det1092_frame_realization_priority_v1.json)
places **class3** immediately after class1. It has the same estimated cost:
a primitive common rank16 core of determinant4100 and auxiliary coordinate
height2. Its witness uses known embedding25, new embedding24 and sixth index155.
Class3 is distinct from class1 and the recovered curve302 frame, class6, in
the complete integral-isometry census. This ranking uses no specialization data.

Only after the completed null class1 panel does the detached transition attempt
class3 realization. It searches the139 retained generic norm12 representatives
by coordinate L1 complexity then mask for an exact target-frame isometry.
The bounded producer then checks the full rational NS transport, effective
primitive nef fibre and rational zero, compiles the exact elliptic equation
and inverse maps, and recovers17 sections and their exact height Gram.
The separately written JSON checker must pass before any parameter search.
The original class1 producer/certificates stay unchanged.

Limits per realization stage:120seconds for lattice discovery/marking/planning
and normalization/compaction,600seconds for trace, equation and section
compilation,180seconds for independent replay, with3GiB RSS per stage. Every
stage saves logs, checkpoints and child CPU. Failure remains UNKNOWN; the
controller neither broadens an unsuccessful realization nor launches an
uncertified parent. Arithmetic strict-class construction remains optional and
UNKNOWN. A J2 realization does not claim a J1 classification or a new K3.

## Smaller matched commissioning panel

After the exact new-parent replay, score the **same65,536 rational addresses**,
using the same primes5..997, control hash and score formula. Take32 ranked
fibres and8 controls. The controls are the identical first8 baseline addresses;
the ranked arms match selection rule and score percentile exposure, not
necessarily numerical parameters. Compare the first32 ranked and8 control
class1 endpoints for equal-sized exposure; keep the remaining baseline visible.
No historical exceptional parameters or saved points enter the new panel.

The ordinary budget remains24 initial calls,256 at≥20,1024 at≥23,2048 at≥25
and8192 at≥27, with reconciliation after every batch. Record realization CPU
separately alongside full cold scoring and per-parameter search CPU. Use their
sum when comparing total cost, rather than reporting only warm point search.
The smaller panel does not automatically expand when it ends.

## Operation and evidence

Class1 live folder:
`research/artifacts/local/elliptic-curves/class1-prospective-v2/`.
Its `commissioning-cap.json`, `commissioning-result.json` and `matched-baseline/`
are the amendment, eventual decision and frozen baseline. Completion exports
the compressed full score table and results into
`research/artifacts/generated-results/elliptic-curves/class1_commissioning_64_16_baseline_v1/`.

The source-frozen detached transition is
`research/artifacts/local/elliptic-curves/x1092-class3-transition-v1/`.
Its `STATUS.json`, stage receipts and eventual `commissioning-panel/` retain
progress and limits. The controller is
[run_x1092_frame_transition.py](../cas/run_x1092_frame_transition.py), with
[producer](../../elkies-k3/scripts/realize_x1092_next_frame.sage) and
[separate checker](../../elkies-k3/scripts/verify_x1092_next_frame.sage).

Eight checks pass: existing scoring/control tests plus exact arm-prefix
quotas, fail-closed gain/null/incomplete branching, and complete synthetic
controller runs confirming the cap, score preservation and gain veto. The
synthetic worker checks scheduling, not mathematical certification. Class3 is not declared
rationally realized merely because its abstract frame appears in the census;
its successful exact replay is the required evidence.
