# Completed new-direction follow-up on the higher-height rank26 curve

The [new rank-at-least26 curve](HIGHER_PARAMETER_RANK26_2026-09-06.md) at
`11952`, `7460/32309`, completes a second separately frozen301-centre wave.
The union of initial49, first adaptive301 and second adaptive301 charts
contains965 retained points. Exact finite checks modulo2,3,5 still certify26.
No new rank27/28 or upper bound follows.

Every new centre has an odd coefficient of the26th point in the ordered
26-point subgroup. All earlier centres had zero coefficient there. The fixed
301 generic labels are paired cyclically with quotient words `256+w`, where
`w=0..255` is ordered by Hamming weight then integer. This gives disjoint
centre parity labels, not an exhaustive quotient or specialized-maxima proof.
Height125000 and ten seconds per chart are unchanged. All301 boxes, admission
histories, exact rational maps and full-cloud provenance replay.

Version1 preflight referenced an incorrect seed-folder name. It failed before
writing a protocol or starting maps/search. The source and a hashed failure
record are retained. Version2 references the completed first-wave seed and
freezes a new protocol before execution. The corrected wave has no censored
boxes; the combined map/search launch took286.876 seconds and the exact
history replay9.337 seconds on the recorded machine.

The [coverage certificate](../../artifacts/generated-results/elliptic-curves/higher_rank26_direction_11952_069_adaptive_coverage_v1.json)
binds both point-cloud proofs and all underlying inputs. The curve remains
`new-20260906-99` in the100-curve inventory. No equation, ID or lower bound is
changed by this completed follow-up.
