# Newfamily historical replay checklist

This is an obsolete source-recovery checklist, not a replay suite or a claim in
[`MATH_STATUS.json`](../../MATH_STATUS.json).  Its proposed six-script suite and
named `/tmp` exploratory files are not present in the checkout.  No result may
be inferred from the checklist's requested outputs, including its generic
section, orbit, genus, rank, or rational-lift proposals.

The current reproducible Newfamily entry point is the
[`cas/newfamily` workflow](../cas/newfamily/README.md).  The sole status-linked
Newfamily result is the [exact rank-14 specialization at $T=83/6$](NEWFAMILY_RANK14_T83_6.md)
with its generated certificates.  Its exact rank applies to that specialization
only; it does not complete the historical checklist's broader construction
claims.

The original checklist, proposed equations, script names and promotion gate are
preserved byte-for-byte in the
[archive](../../archive/elliptic-curves/notes/NEWFAMILY_REPLAY_CHECKLIST.md.txt).
Do not recreate missing `/tmp` inputs, run a replacement scan, or promote a
generic assertion from that archive without an independently scoped source and
certificate plan.
