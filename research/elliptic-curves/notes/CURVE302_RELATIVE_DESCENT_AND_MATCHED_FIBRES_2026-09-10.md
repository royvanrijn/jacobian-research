# Curve302: relative ideal classes, the seed obstruction, and matched fibres

The ten strict half-ideal classes of302 are independent of **all eight
inherited everywhere-even half-ideal classes**. Their union is a split
elementary18-dimensional factor of the ordinary class group. The full
fourteen-dimensional displayed quotient contributes between12 and14 new
ordinary ideal-class dimensions; its remaining unit ambiguity is at most2.

The enlarged Artin calculation resolves the two historical seed norm
equations: **both have no integer solution**. The normalized first seed
therefore supplies an ideal class outside the full inherited image.

Seven of ten frozen comparison fibres have independently checked local,
strict, and ordinary-unramified point-subspace anatomy. Three retain
incomplete prime support. These are subgroup rank lower bounds, not exact
elliptic ranks or completed class groups. The four-part programme remains open.

This is the current extension of the preserved
[strict-block proof](CURVE302_DESCENT_ANATOMY_2026-09-10.md) and
[two-norm-equation reduction](DET1092_SEED_VIRTUAL_UNIT_AND_IDEAL_PARITY_2026-09-09.md).
Their frozen inputs remain at their original paths; their next-step and
unresolved-principality paragraphs describe the earlier checkpoints.

## Spaces and the relative ideal map

Keep the exact cubic marking and maximal order of the strict-block note.
Let \(W\) be the31-dimensional cubic Kummer image of the public subgroup,
\(G\subset W\) the specialized saturated generic MW17 image, and
\(V\subset W\) the ten-dimensional kernel of joint localization at the
retained bad/model primes and infinity. Thus \(G\cap V=0\).

Write \(T_{\rm ev}\) for the classes in \(T\) with even valuation at every
finite prime ideal, and \(U_T\) for those defining quadratic extensions
unramified at every finite prime and split at every real place. The latter
are characters of the **ordinary** class group.

| Space | All classes | Everywhere even | Ordinary unramified | Strict |
|---|---:|---:|---:|---:|
| Inherited \(G\) |17|8|6|0|
| Known \(W\) |31|22|20|10|

For an everywhere-even representative \(\beta\), define
\(\Phi([\beta])=[J]\), where \((\beta)=J^2\). The virtual-unit exact sequence
identifies its kernel with unit squareclasses. All classes here have
square rational norm. In a totally real cubic, norm-positive unit
squareclasses have dimension2: Dirichlet gives dimension3 for all unit
squareclasses, and the norm sign is onto since \(N(-1)=-1\).

Construct all20 ordinary unramified characters in \(W\), then pair them
with the eight frozen inherited and ten frozen strict half ideals.
The exact20-by18 matrix has block ranks

\[
\operatorname{rank}A_G=8,\qquad
\operatorname{rank}A_V=10,\qquad
\operatorname{rank}(A_G\mid A_V)=18.
\]

Thus \(\Phi|G_{\rm ev}\) and \(\Phi|V\) are injective, with disjoint images
even in \(\operatorname{Cl}(K)/2\). Their order-two representatives and
these characters give a retraction, proving a split
\((\mathbf Z/2)^{18}\) factor. The earlier narrow ten-dimensional conclusion
for \(V\) persists. We do not assert a narrow18-dimensional factor from
the possibly nonpositive inherited generators.

Append four canonically selected even words to obtain a22-word basis of
\(W_{\rm ev}\). Their additional half ideals have verified square identities.
The complete20-by22 Artin matrix has **rank19** and a three-dimensional
right kernel. This gives a split ordinary elementary19-dimensional factor.
Its restriction to the half ideals of \(U_W\) also has rank19.

The unit-kernel bound2 sharpens what this finite pairing detects:

\[
\boxed{20\le\dim\Phi(W_{\rm ev})\le22,\qquad
12\le\dim\bigl(\Phi(W_{\rm ev})/\Phi(G_{\rm ev})\bigr)\le14.}
\]

For an unknown \(u\in\{0,1,2\}\), these dimensions are exactly \(22-u\)
and \(14-u\). The matrix packets retain their earlier weaker rank-only
intervals; the unit theorem sharpens them as above.

The20 independent ordinary characters also prove
\(\dim\operatorname{Cl}(K)/2\ge20\). Neither this nor the rank19 pairing
determines the full group, the missing characters, or an order-four class.
A zero value in a finite character test does not establish principality.
One reduction of each of the seven nonzero Artin-kernel words returned
no unit ideal. That finite diagnostic settles none of those seven
principality questions; its checkpoints are retained locally.

Since \(\dim U_W-\dim U_G=20-6=14=\dim W/G\), inclusion induces

\[
U_W/U_G\simeq W/G.
\]

Every displayed exceptional direction can be corrected by an inherited
point class to become an ordinary unramified character. This is
retrospective: it neither constructs points from characters nor eliminates
additional Selmer/Sha classes. The earlier exact identity remains
\(\dim\operatorname{Sel}_2(E_{302})=21+c_S\), with \(c_S\ge10\) and
its exact value **UNKNOWN**.

## Both historical seed norm equations are obstructed

The preserved reduction proved that the normalized first-seed half ideal
\(J_*\) belongs to the inherited image if and only if at least one of
two explicit integral norm equations \(F_0=1,F_1=1\) has an integer
solution. The two candidate relative ideal classes are

\[
R_0=J_* /(G_1G_2G_4),\qquad
R_1=J_* /(G_0G_1G_2G_3G_5G_7).
\]

Here \(G_i\) denotes the eight inherited half ideals, indexed from zero;
the saved principal reduction multipliers are retained. The earlier
seven characters prove these exhaust all compatible inherited corrections.

Let \(\chi_k\) be the zero-indexed new20-character basis. The complete
20-by2 evaluation gives, in particular,

\[
\chi_0(R_0)=1,\qquad \chi_2(R_1)=1.
\]

Neither ideal is principal, so **neither \(F_j=1\) has an integer solution**.
Their explicit rational points and everywhere local integral solubility
remain valid. This is a global Artin obstruction to principality.

The exhausted correction list proves \([J_*]\notin\Phi(G_{\rm ev})\).
The argument also excludes membership modulo2, so the eight inherited
ideals and seed give nine split elementary factors. The seed's
unit-versus-ideal fork is closed in favour of a new ideal class.
The detecting characters use the known31-point group; no prospective
seed selection follows.

## Ten fixed comparison fibres, seven completed local anatomies

The roster was frozen before descent from the existing60-fibre panel.
Each of five pairs shares its literal R17 fibration; parameter heights
and search exposure differ. These controls lie on the earlier
determinant948 surface;302 is a separate determinant1092 reference.
Matching is limited to the within-pair fibration. This is neither a random
sample nor a prospective predictor test.

Here \(n\) is the subgroup lower bound, \(v_G,v_W\) are strict dimensions,
\(u_G,u_W\) ordinary-unramified dimensions, and \(\ell\) the full Selmer
localization-image dimension. For every completed row,

\[
\dim\operatorname{Sel}_2=\ell+c_S,\qquad c_S\ge v_W,
\]

with full \(c_S\) unknown. Dashes mean missing support, not zero.

| Pair / fibre | \(n\) | \(v_G,v_W\) | \(u_G,u_W\) | \(\ell\) | Relative ideal lower bound |
|---|---:|---:|---:|---:|---:|
| Reference302 |31|0,10|6,20|21|12|
| 1 / 103b2-low-02 |27|7,17|11,21|10|9|
| 1 / 103b2-high-01 |17|2,2|10,10|15|0|
| 2 / 103b2-low-05 |26|6,15|11,20|11|7|
| 2 / 103b2-high-05 |17|—|—|—|—|
| 3 / 074d9-low-04 |26|2,10|8,17|16|7|
| 3 / 074d9-low-02 |21|—|—|—|—|
| 4 / 11952-low-04 |26|0,8|8,17|18|7|
| 4 / 11952-high-02 |21|6,10|11,15|11|2|
| 5 / 08f72-low-01 |21|—|—|—|—|
| 5 / 08f72-high-02 |17|4,4|11,11|13|0|

The completed controls' exact \(G_{\rm ev},W_{\rm ev}\) dimensions are
respectively \((12,22),(12,12),(13,22),(10,19),(10,19),(13,17),(13,13)\).
Their relative ideal lower bounds subtract the norm-positive unit
dimension from \(\dim W_{\rm ev}-\dim G_{\rm ev}\), clipped at zero.
That unit dimension is1 for the complex cubic of103b2-low-02 and2 for
the six totally real cubics. These bounds do not determine the exact
inherited ideal images or unit kernels on controls; full half-ideal/Artin
replication remains to be done.

For each completed \(y^2=f(x)\), the derivative squareclass
\(-\operatorname{disc}(f)f'(\theta)\) has square norm and is unramified
outside the complete retained support. The independent checker finds
a place where it lies outside the full local point image. Local Tate
self-duality and Hilbert reciprocity supply a nonzero constraint on the
product of local point images. The known joint image has codimension one
in that product, proving it is the entire Selmer localization image.
The strict class-field identification gives the formula above, using
the [strict class-field proof](../rank-jump/STRICT_SELMER_AND_ARTIN_BLOCKS.md)
and [reciprocity argument](../rank-jump/DERIVATIVE_RECIPROCITY_AND_COMPLETE_BOUNDARY.md).

The original residue-degree-one dyadic implementation stopped on
11952-high-02. Its separate completion uses the full unit group modulo
\(P^{2e+1}\) at the degree-three dyadic prime. The independent checker uses
complete Hilbert-dual squareclass bases at every prime for all seven
curves, verifies that each Hilbert Gram matrix has full rank, and agrees
with the producer's local ideal-log coordinates. The original incomplete
packet is retained.

Three discriminant-support cofactors, of126,113,113 digits, remain
unfactored by the retained evidence. No new factorization was started;
the exact cofactors and all ten inputs remain in the protocol and
verification certificate. Missing rows were not replaced.

The witnessed ordinary-character count20 on302 is equalled by one
rank-at-least26 control and exceeded by the rank-at-least27 control's21.
The latter's strict count17 also exceeds302's10. These raw
**known-point-derived lower bounds** do not distinguish302 uniquely;
they do not compare actual full class-group sizes. All seven completed
controls also satisfy \(\dim U_W-\dim U_G=n-17\). Thus ordinary unramified
correction of the whole displayed quotient occurs throughout this small
panel. That feature alone does not explain302's exceptional size or
validate a predictor.

## Replay and next proof gate

The [manifest](../../artifacts/generated-results/elliptic-curves/curve302_relative_descent_manifest_v1.json)
pins this note, producers/checkers, transitive input bindings, exact
matrices and the matched-panel packets. From the repository root:

~~~sh
python3 research/elliptic-curves/cas/package_curve302_relative_descent.py --check
timeout 120 sage -python research/elliptic-curves/cas/verify_curve302_relative_ideal_anatomy.sage --negative-controls
timeout 60 sage -python research/elliptic-curves/cas/verify_det1092_seed_artin.sage
timeout 60 sage -python research/elliptic-curves/cas/curve302_seed_norm_artin_obstruction.sage --check
timeout 180 sage -python research/elliptic-curves/cas/verify_rank_jump_matched_descent_panel.sage --check
~~~

The302 replay checks22 ideal squares by integral lattices,440 Artin
entries, restored bad-prime terms, Hensel repairs at47,67,89, and a
degree-two residue-field repair at47. The seed replay adds40 entries
using independent Jacobi evaluation and Hilbert symbols instead of local
square tests, plus the earlier independent norm-form/correction-list proof.
The panel rechecks points, native17 prefixes, complete support, global
mod2 independence, and every local subspace. Sage10.9/PARI2.17.3 was used.
Runs have one worker and60 seconds per control. The302 replay cap is120 seconds;
a repeat panel replay hit120 seconds after six rows, so its final replay uses
a separately recorded180-second cap. The earlier complete seven-row replay,
the timeout and the pre-refinement manifest are all preserved.
No BNF, new point, new parameter, or production-search mutation is included.

**Next priority:** use the explicit class1↔class6 surface maps to transport
the fourteen exceptional points and establish a valid shared-carrier test.
Each point on the302 fibre acquires its own B-base parameter; this is not
a group homomorphism between two fixed elliptic curves. The shared rank16
lattice core alone does not identify points as multiples or translates
of generic B-sections. Those exact identifications, then low-genus
carriers with independent twist directions, are the next geometric gates.

Remaining arithmetic tasks are control half-ideal pairings, three support
completions,302's possible0–2 unit kernel outside the detected18-space,
and full class/Selmer upper bounds. Self-propagation still requires a
point-producing rationality implication and certified independence;
the finite13-of14 seed recovery alone does not supply that theorem.
