# MW17-jump-v2 zero-gain rescue: missing-input boundary

The complete frozen protocol, historical detector record, checkpoint history,
and commands are preserved byte-for-byte in
[the archive](../../archive/elliptic-curves/notes/MW17_JUMP_V2_2026-09-04.md.txt)
(`sha256: 4d30ebecf42348a238a2b862569bdc674eefbb9cd05f89540391df57d415f973`).
It does not authorize a scan, rescue launch, or recomputation of the generic
parity census.

<!-- status-consumer: EC-K3-MW17-JUMP-V2-ZERO-GAIN-RESCUE e5320f1f3bf33148 -->

The canonical [partial claim](../../MATH_STATUS.json) freezes 2,239 addresses,
the exact quotient-gain leaderboard, and a hash-selected 264-row rescue arm.
Historically, 66 leading 07ca9 fibres completed 2,838 initial charts with
clean zero gain; four in-flight shards were stopped without checkpoints and
the remaining 2,173 candidates were never evaluated. This is a bounded
negative signal, never an upper bound or an exclusion of the untested inputs.

## Frozen, unrun rescue

Eligibility requires a clean exact base record with zero gain after all 43
initial charts and no timeout or backend failure. Each eligible assigned fibre
would receive the disjoint next 301 exact generic half-classes, reranked within
that fixed set by specialized depth, in seven 43-chart batches. On a certified
escape, unused slots switch to the existing quotient-adaptive policy; the total
cap remains 344 charts. Of the historical data, 12 clean zeros are assigned,
but **no rescue chart has run** and no gain, rank lower bound, or detector
performance conclusion exists.

## Exact remaining obligation

The current pointed-quartic runtime changes finite coordinate boxes. The four
historical raw-backend chunks prove only the recorded historical zeros; the
separately tagged compatible base chunks expected by the amended runtime are
absent locally. They must be recovered and identity-checked before a separately
scoped finite experiment could be proposed. Missing inputs do not justify
repeating the base search.

The historical builder's literal-hash check is stale after the amendment and
must not overwrite the frozen protocol. The read-only
[commitment audit](../cas/audit_mw17_rescue_commitment.py) checks the frozen
population, assignment, budgets, pinned amendment, and four historical chunk
hashes; it neither imports Sage nor certifies runtime readiness. A passing audit is not a runtime-readiness certificate. The current rescue implementation calls `complete_generic_census` for each
worker; it does not load a persisted complete parity table.

The [frozen manifest](../../artifacts/generated-results/elkies-k3-mw17-jump-v2-campaign-v1.json)
and [merged ledger](../../artifacts/generated-results/elkies-k3-mw17-jump-v2-ledger-v1.json)
retain the compact evidence. Reuse the
[runtime-compatibility method](../../knowledge/ALGORITHMS.md#method-ec-frozen-runtime-compatibility-preserve-frozen-assignments-while-separating-changed-coordinate-measurements)
and leave future work to the [programme map](../README.md).
