{
  "schema": "rank-jump.reference-adaptive-class-completion-protocol.v1",
  "endpoint": "Construct one additional strict class on the smaller +6 reference to validate the global constructor for302. No class-group upper bound or parameter search is sought.",
  "method_change": "The preceding fixed-target wave did not skip coordinates solved by earlier targets. Rebuild its equation-only relation matrix, then at each declared strip level enumerate the still-free good degree-one ideal coordinates and skip any coordinate that becomes a pivot before its target is processed. Never protect point-derived anchors. Monitor exact strict-class extraction after each64 completed targets and after each level. Stop on an independently detectable strict class outside G, not on a target class-group dimension.",
  "inputs": "The four eligible early box waves and five eligible early strip waves, the completed rank-jump fixed-target wave, the six extra seeded relations, and the frozen equation/generic projection. Protected/capped/residual point-character waves are excluded. All choices depend on the equation and accumulated principal relations.",
  "bounds": {"workers": 1, "worker_seconds": 1800, "pari_stack_bytes": 268435456, "strip_levels": [16384,32768,65536], "targets_per_level_max": 1024, "smooth_bound": 50000, "monitor_interval": 64, "new_parameters": 0, "elliptic_point_searches": 0},
  "checkpointing": "Exclusive per-target and per-monitor snapshots; preserve skips, completed-target history, population digests, terminal reason and all accepted elements. A timeout retains finished chunks. This frozen run has no larger restart.",
  "verification": "Norm and principal-ideal identities are checked on every retained element. The existing complete outside-S parity and local-square extractor is applied to immutable snapshots, with all generic classes included. A detected class is only a candidate until independent ideal/local/independence replay. No hypothetical presence, heuristic score, missing relation count or resource failure is a class certificate.",
  "scope": "A bounded one-field class-construction calibration.302 remains the primary mechanism target. No changes to Agent1's live search files, population or resource limits."
}
