# Elliptic-curve cleanup status — 13 September 2026

[MATH_STATUS.json](../MATH_STATUS.json) and its canonical sources are the
mathematical authority. This is a navigation report; it does not restate
theorems or authorize a calculation.

## Start here

- [Programme map](README.md): current entry points and active fronts.
- [Method memory](../knowledge/ALGORITHMS.md): reusable algorithmic lessons
  and routes that must not be repeated.
- [Partial-result review](../knowledge/PARTIAL_REVIEW.md): all **115/115**
  current partial results have their exact missing gate and retained evidence
  boundary.
- [Curve inventory](INVENTORY.md): generated curve and conductor totals;
  dated cohort totals remain historical snapshots.
- [Canonical-source index](../index/elliptic-curves.md): all current sources,
  including compact historical boundaries and their preserved originals.

## What changed

Historical runbooks and experiments now state whether they are a completed
bounded result, an unscheduled control design, or a missing-input boundary.
Their proofs, receipts, scripts, and replay inputs remain preserved. Dated
commands, PIDs, queues, and continuation cursors are not work authorization.
The repository-hygiene audit pins fourteen dated EC/K3 handoffs to those
historical boundaries, including the obsolete NS0024 and completed
second-prime/MW16 campaign queues.

The software-lock audit records the status-linked programs, inputs,
certificates, checkpoints, and support records. The canonical-artifact audit
currently finds all **350/350** registered generated artifacts referenced.
Three reviewed canonical sources intentionally have no registered generated
artifact because their evidence is a written proof or finite regression. These
are integrity and navigation checks, not proof-sufficiency claims.

The level-474 H3 rational-base result now has an independent Sage
bielliptic-quadratic-Chabauty certificate. The historic Magma route remains
preserved as provenance; it is no longer the current proof boundary.

The current map preserves the useful operational conclusions: reuse sealed
compatible landscapes; distinguish bounded exposure from a rank or
point-absence claim; keep missing replay inputs explicit; and measure the
whole search-plus-verification path before calling an optimization cheaper.

## Remaining EC cleanup work

No search, descent, point construction, factorization, Sage replay, or
controller restart was performed for cleanup. Existing certificates prove only
their stated scope; a bounded miss is never an upper bound.

The inherited review has **36 completed snapshot records** and **41 retained
open records**. Seventeen have active elliptic-curve/supporting-K3 review or
provenance destinations; the other 24 belong to archived programmes and remain
historical obligations, not current work.

For elliptic curves and retained supporting K3 claims, the unfinished items
are grouped by their current gate:

- **Semantic authority review:** `241`, `243`, `245`, `247`, `256`, `264`,
  `268`, `665`, `1038` — source-by-source checks of theorem scope,
  dependencies, canonical-source uniqueness, checker role, checker modes, and
  assurance evidence.
- **Replay provenance:** `297`, `1046` — sufficient source/input/output
  packaging or an explicit immutable-source boundary.
- **Navigation:** `1024`, `1030` — maintain short active boundaries and label
  historic prose whenever a later theorem closes its route. Active canonical
  sources are checked against the stale execution headings that previously
  made completed experiments look like current work.
- **Separately scoped mathematics:** `309`, `321`, `323`, `375` — new work
  only after a concrete compute/replay scope is approved; `UNKNOWN` remains
  unchanged until its certificate closes.
  - `309` packages a historical H3 q6-to-q8 chain with three absent inputs; it
    does not affect the independent current H3 point proof.
  - `321` has complete retained-witness transport replay for seven fibres;
    exhaustive cover and companion modes remain separately unscheduled.
  - `323` has 15 exact displayed-subgroup transports; the other 54 dimensions
    are explicit `null`/`UNKNOWN` values.
  - `375` has a compact smooth-character replay and checked singular mergers,
    but the retained summaries omit the polynomial data needed for an
    independent singular replay.

The archived `285` and the other archived entries are preserved under
[non-elliptic history](../archive/non-elliptic/README.md). They are excluded
from this report's current work list without being misrepresented as resolved.
The archive audit also requires every active claim to use an elliptic-curve,
supporting-K3, or shared-replay canonical source; archived labels and sources
cannot silently become part of the active programme.

Proof-assurance flags are evidence states, not cleanup defects. The generated
[work ledger](../knowledge/WORK_LEDGER.md) contains the current EC queue.
Non-elliptic programmes are in the [archive](../archive/non-elliptic/README.md).

## Validation

`make check-navigation` checks status, source reviews, software locks,
artifact references, Markdown links, and whitespace. It does not replay
historical arithmetic.
