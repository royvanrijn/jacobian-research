# H3 q24 equation-D13 -> pinned R17 closeout

Date: 2026-08-23

Status: **PASS exact integral lattice / NS / marking path from the current equation
D13 frame to pinned recovered R17**.

Primary verifier:

```text
elkies-k3/scripts/certify_h92_q24_equation_d13_to_pinned_r17.sage
```

Terminal status:

```text
PASS_Q24_EQUATION_D13_TO_PINNED_R17_LATTICE_PATH
```

## Result

The current equation D13 frame and the older pinned/dominant D13 search frame are
different markings of the same Neron--Severi lattice.  Their standard embedded `U`
planes are not literally the same, so selected fibre classes must be transported
through the exact marking change rather than compared as raw H3-coordinate vectors.

The verifier reconstructs that full 19-dimensional marking map using the current
passing transport stack, then transports the selected q24 orbit-85 fibre into the
equation D13 coordinates.  Its child basis is chosen as the already-certified
R17-directed D12 frame.

The exact suffix is then replayed:

```text
D13
 --q24 orbit 85--> D12/MW5
 --q6  orbit 42--> A11/MW6
 --q8  orbit 922--> 2A5/MW7
 --q4  orbit 472--> 3A3/MW8
 --q4  orbit 323--> A3+2A2/MW10
 --q4  orbit 207--> 5A1/MW12
 --q4  orbit 52 --> 4A1/MW13
 --q4  orbit 114--> 3A1/MW14
 --q4  orbit 498--> 2A1/MW15
 --q4  orbit 981--> A1/MW16
 --q6  orbit 2247--> rootless/MW17
```

Every selected arrow has old-fibre degree two.

The final rootless frame is identified with

```text
elkies-k3/data/lattice/rank17_gram.txt
```

by the exact stored determinant-one endpoint isometry.

## Final-step certificate

The previously missing native endpoint statement is now explicit:

```text
Q24R17MAP_FINAL_Q6|
parent=A1/MW16|
q=6|
orbit=2247|
degree=2|
child=rootless/MW17|
status=PASS
```

and the complete endpoint check reports

```text
Q24R17MAP_PINNED|...|pinned_R17=1|status=PASS
Q24R17MAP_RESULT|...|status=PASS_Q24_EQUATION_D13_TO_PINNED_R17_LATTICE_PATH
```

Thus the H3 route is now closed from the **current equation D13 marking** to the
original recovered 17x17 endpoint at lattice/NS/marking level.

## Alternate first-hit suffix

A separate fresh search from a minimized q24 D12 frame found

```text
55, 1960, 261, 567, 3822, 8598, 13487, 18368, 17593
```

through `A1/MW16`.

That branch is mathematically valid but is not the R17-directed suffix.  Its A1 has a
different endpoint geometry; a q6 rootless child at orbit 504 is not pinned R17, and a
direct pullback of pinned R17 into that A1 has enormous old-fibre degree.

The lesson is that matching the ADE/MW sequence is insufficient.  The transported
elliptic marking must be retained.

## Historical continuation boundary

This closeout originally proved the lattice/marking path only.  The former
equation-work list is superseded: the selected q24 `D13 -> D12` and q6
`D12 -> A11` equation edges are complete, and the q8/orbit376 plus
q12/orbit5867 route reaches the source-identified rootless rank-17 endpoint
with its saturated basis.  See
[`H3_Q24_FRONTIER_2026-08-23.md`](H3_Q24_FRONTIER_2026-08-23.md),
[`ORBIT42_EQUATION_LIFT.md`](ORBIT42_EQUATION_LIFT.md), and
[`PRIOR_WORK_SHORTCUT_AND_ENDPOINT_CERTIFICATION_2026-08-25.md`](PRIOR_WORK_SHORTCUT_AND_ENDPOINT_CERTIFICATION_2026-08-25.md).

The exact direct-chart exclusion for curve273 remains separate: it has no
rational parameter in the published rootless family.  A construction for that
curve still requires a different family, an isogeny-level explanation, or a
source certificate; this historical suffix is not a queue to rerun.

## Reproduction

From a checkout with the current prerequisites:

```bash
sage -python elkies-k3/scripts/certify_h92_q24_equation_d13_to_pinned_r17.sage
```

Expected terminal status:

```text
PASS_Q24_EQUATION_D13_TO_PINNED_R17_LATTICE_PATH
```
