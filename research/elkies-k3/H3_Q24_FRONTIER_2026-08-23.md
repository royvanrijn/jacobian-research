# H3 q24 resolved-component frontier — 2026-08-23

This note records the current equation-level frontier for the selected H3
`D13/MW4 --q24--> D12/MW5` neighbour.  It is a research checkpoint, not a
replacement for `MATH_STATUS.json`.

## Certified inputs

The selected q24 divisor is already transported exactly into the current D13
equation frame.  Its generic-fibre/vertical profile is

```text
old-fibre degree = 2
P24.O            = 24
height(P24)      = 52
D13 correction   = 0
vertical fibre   = -7 F
D13 root coeffs  = 9,7,1,2,3,4,5,6,7,5,8,4,8
```

The modular degree-two RR preflight at `p=100003` is exact for the declared
ambient and smooth-collision quotient:

```text
ambient dimension        = 58
smooth-collision rank     = 48
post-collision dimension  = 10
required resolved rank    = 8
required final h0(D)      = 2
```

The ordinary cusp-jet map on that 10-dimensional space has ranks
`1,2,...,10,10,10` through jet orders 1..12.

## Actual I9* resolution

The canonical D13 parent has local orders `(2,3,15)` at its rational `I9*`
place.  Explicit ordinary blow-up charts give

```text
12 blow-up centres
13 geometric exceptional components
```

because centre `C10` has a rank-two quadratic tangent cone and therefore
contributes two geometric exceptional branches.  The current resolution
artifact terminates with

```text
PASS_EXPLICIT_MODP_I9STAR_D13_COMPONENT_RESOLUTION
```

Thus the previous `12 != 13` mismatch was a centre/component accounting bug,
not a failure of the D13 parent.

## Ordinary-jet shortcut rejected

The first eight ordinary cusp jets cut the post-collision 10-space to dimension
2, but the resulting squarefree chord radicand has degree 18, hence is not a
genus-one pencil.

A complete scan of all 45 coordinate two-planes in the ten-jet space gives
only squarefree degrees 18 or 20 (genera 8 or 9).  No monomial jet pair gives
genus one:

```text
PASS_NO_MONOMIAL_JET_PAIR_GENUS_ONE
```

Therefore the true resolved `I9*` quotient is a non-coordinate linear subspace
of the ordinary ten-jet space.  Dimension counting or ordinary jet vanishing
cannot substitute for the connected resolved-component module.

## Current next gate

Run

```bash
sage -python elkies-k3/scripts/certify_h92_q24_i9star_component_graph_modp.sage \
  --prime 100003
```

This reconstructs the affine D13 dual graph from the blow-up chronology,
recovers the Kodaira fibre multiplicities from its affine intersection matrix,
matches the geometric D13 root graph to the deterministic lattice root basis
used by the exact q24 transport, and pushes `-7F+V_D13` to explicit geometric
component coefficients.

The D13 graph has the usual twofold spinor-arm automorphism.  If the two
transported spinor coefficients differ, graph data alone cannot orient those
two branches.  The resolved RR compiler must then carry both orientations
until actual branch/trivialization incidence selects one.

After that, the proof task is to pull the ten post-collision functions through
the actual charts, derive the connected component quotient, and require

```text
resolved quotient rank = 8
h0(D)                  = 2
squarefree quartic deg = 4
child root lattice     = D12
MW rank                = 5.
```

Only then should the q24 equation arrow be promoted and lifted to the exact
characteristic-zero D12 child.
