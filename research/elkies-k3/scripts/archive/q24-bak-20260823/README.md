# q24 backup snapshots from 2026-08-23

These files are timestamped `*.bak-*` snapshots moved out of the top-level
`elkies-k3/scripts/` directory during the fixed-corridor cleanup.  The final
eleven stragglers were relocated on 2026-09-02; their original paths and
archived-byte hashes are recorded in [`MANIFEST.tsv`](MANIFEST.tsv).

They are not current proof or compiler entry points. Use the root-level scripts
named in `elkies-k3/scripts/README.md` for the selected q24/orbit85 to pinned
R17 corridor.
