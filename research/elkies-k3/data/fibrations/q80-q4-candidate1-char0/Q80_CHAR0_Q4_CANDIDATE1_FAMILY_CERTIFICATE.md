# Q80 q4 candidate1 — characteristic-zero exact family certificate

Status: **PASS_EXACT_Q4_CANDIDATE1_FAMILY**

- exact RR survivors compiled: `4`;
- exact target children: `2` at indices `[2, 3]`;
- target fibre pattern: `I2 + I3 + I4 + I5 + I6 + 4 I1`;
- root lattice: `A1+A2+A3+A4+A5`, rank `15`, determinant `720`;
- infinity smooth;
- deterministic continuation representative: index `2`;
- target branches with the identical displayed `j(W)` map as the selected branch: `[2, 3]`.

All exact target branches are retained in the JSON artifact; selection of the smallest index is a continuation convention, not a uniqueness claim.
