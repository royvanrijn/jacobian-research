# A genuine branch half without loss of any inherited Mordell–Weil direction

**Exact arithmetic distinction on the retained rank18 control.** At the quadratic
branch field of the Q80 bisection alternate-orbit-11ae6, specialization of the
full generic MW17 group is injective. Its branch point P satisfies

```
2P = specialize(P16-P1),
```

but P is not the specialization of any generic section. Moreover the first
halving enlargement of the inherited image has dimension exactly1 over F2,
generated by this point. Thus the known common half is a genuine divisibility
defect, rather than a relation among inherited sections that vanishes at the
branch value.

This concerns the branch fibre over a quadratic number field. P is in the
rational span of the17 inherited directions, so this does not prove number-field
rank18 there. The [quadratic function-field cover](Q80_ONE_COVER_HALVING_CONTROL_2026-09-14.md)
was already proved to have exact rank18; no second gain is claimed.

## 1. Integral specialization injectivity from modulo3 quotients

Let K=Q(alpha), where alpha is a root of the retained quadratic branch polynomial,
and let M=E(Q(t)) be the certified full free group with its17-section source
basis. The new finite certificate examines simple degree-one places of K at
primes5..997, stopping when the combined quotient matrix has rank17 over F3.
Every retained place has a smooth integral parent fibre.

At each place the checker enumerates the entire finite elliptic group, its
subgroup of triples, and the quotient cosets. The17 reduced sections give
columns in that quotient, of dimension at most2. Combining the15 selected
places gives rank17. The selected prime set is

```
23,89,103,127,131,157,191,223,229,241,277.
```

At prime43 the branch polynomial has the simple root27 and the smooth elliptic
group has order44. Prime-to43 torsion reduction therefore proves E_alpha(K)[3]=0.
The certificate verifies the polynomial's unit leading coefficient and simple
root condition, so this is an actual degree-one place of the quadratic field.

Suppose an integer relation vector v specializes to zero. The combined modulo3
matrix forces every coefficient of v to be divisible by3, say v=3w. Absence of
K-rational3-torsion makes w another relation. Repeating proves v=0. Hence

```
M -> E_alpha(K) is injective.
```

This conclusion has no section-height bound. It does not assert saturation at
all primes or determine the full number-field rank.

## 2. An exact half outside the entire inherited image

In the retained expression

```
x(Q)=x0(t)+x1(t)*w,   y(Q)=y0(t)+y1(t)*w,   w^2=q(t),
```

the branch point has coordinates P=(x0(alpha),y0(alpha)). The checker verifies
the elliptic equation and the displayed doubling identity in Q[t]/(q), using
the actual source sections P1 and P16. All reduced rational coordinates are
retained in the new result, with source hashes.

Write H for the image of M in E_alpha(K). If P belonged to H, say P=sp(R), then

```
sp(2R-(P16-P1))=0.
```

Injectivity would imply 2R=P16-P1 in M, impossible in the full source basis.
Thus P lies outside H. This excludes every generic integer combination, not
only the [previous signed basis contact bank](INHERITED_QUADRATIC_CONTACT_GATE_2026-09-15.md).
Since its double is a nonzero member of H, P is nontorsion but introduces no
new rational vector-space direction in this specialized fibre.

## 3. Exactly one first halving class

The prior branch-field certificate proves

```
ker(M/2M -> E_alpha(K)/2E_alpha(K))
    = span(P16-P1 mod2M),
E_alpha(K)[2]=0.
```

These are inherited exact inputs, not consequences of the new modulo3 test.
Define H^[2]={R in E_alpha(K):2R in H}. Since specialization is injective and
there is no2-torsion, doubling induces an isomorphism

```
H^[2]/H  ->  (H intersect 2E_alpha(K))/2H.
```

The latter is the displayed one-dimensional kernel. Consequently

```
H^[2]=H+Z*P,     [H^[2]:H]=2.
```

This is the first halving step only. No bound on repeated halving, the full
2-primary saturation index or the full number-field MW group is asserted.

## 4. What the distinction changes for correlated gain

At an arbitrary branch value a nonzero specialization kernel modulo2 can arise
from an inherited relation becoming zero, from an actual new half outside the
inherited image, or both. The two mechanisms should be separated before choosing
a contact search. The previous basis-derived half points used vanishing relations;
the present control certifies the second mechanism while retaining all17 inherited
directions.

At a branch where specialization is injective and no rational2-torsion exists,
two independent common trace parities would require two independent first-halving
classes. This control has exactly one, and the earlier exact rank18 theorem
already excludes a second gain on its fixed cover. Changing inherited words
cannot supply a second class here.

The control's trace is of the F1 quotient type from a smooth rational bisection.
The quadratic point just proved noninherited is therefore not an F0/F2 contact
input for the arithmetic-genus-three constructor. It supplies arithmetic mechanism
evidence, not a new eligible carrier. A different branch incidence with sufficient
independent classes and global lifts remains necessary for the
[requested two-gain construction](CORRELATED_QUADRATIC_GAINS_2026-09-12.md).

## Evidence and verification boundary

- [Selected residue places and source bindings](../artifacts/generated-results/elkies-k3-q80-branch-saturation-defect-v1/input.json)
- [Exact half and rank certificate](../artifacts/generated-results/elkies-k3-q80-branch-saturation-defect-v1/result.json)
- [Replay](scripts/verify_q80_branch_saturation_defect.py)

```
.venv/bin/python research/elkies-k3/scripts/verify_q80_branch_saturation_defect.py
```

The ascending-prime producer is retained under preflight; it and the replay use
20 CPU-second and1GiB caps. No point-height search or research census was run.
Finite group arithmetic and the exact quadratic-field identity are replayed;
the integral descent and first-halving interpretation are written arguments.
No independently written whole-proof replay, formal verification or external
review is claimed.

During replay development, polynomial-zero comparisons were made explicit with
SymPy's is_zero property, avoiding equality comparisons between zero polynomials
with different coefficient domains. The same correction was applied to the prior
inherited-contact check's nonzero ordinate test; that certificate still passes
and its mathematical result is unchanged.
