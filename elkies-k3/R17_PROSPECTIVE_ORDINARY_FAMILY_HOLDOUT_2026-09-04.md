# R17 prospective ordinary-parameter family holdout

<!-- status-consumer: EC-K3-R17-NORM12-HIGHEST-RANK-TRANSPORTS c4c8a81fc735fea2 -->
<!-- status-consumer: EC-K3-R17-NORM12-PROSPECTIVE-FAMILY-HOLDOUT 8fb7417663ea1d98 -->

## Status

Two corrections are now pinned.

First, the 69 recognized ICARM fibres are a public-success endpoint, not a
search population.  The repository has no historical count or parameter law
for failed trials, so neither family success rates nor search effort can be
recovered from the 69 rows.  The public table remains calibration data only.

Second, the three highest-rank unresolved rows now have exact quotient
transports.  Curves 11 and 423 specialize the saturated generic MW17 basis on
`norm12-orbit-08234`; curve 391 does so on `norm12-orbit-07ca9`.  All three
public 28-point subgroups are certified independent by exact finite
reductions, every recovered section relation is verified by the exact group
law, and Smith reduction gives

\[
 \langle P_1,\ldots,P_{28}\rangle/\operatorname{MW}_{17}^{\rm spec}
 \cong \mathbf Z^{11}.
\]

This raises the exact displayed-quotient count from 12 to 15.  The other 54
rows remain literal `UNKNOWN`.

## Highest-rank native transports

The complete height-12 section-planning gate selects a determinant-one child
basis on both missing representatives:

| family | old degree-one sections | glue bisection | determinant | public fibres |
|---|---:|---|---:|---|
| `norm12-orbit-08234` | 16 | `orbit-05a41` | `+1` | 11, 423 |
| `norm12-orbit-07ca9` | 16 | `orbit-07cad` | `-1` | 391 |

The plan is complete through the Cauchy bound: height ten would give a root
in the old rootless MW lattice, and height twelve is the trace vector itself.
The direct compilers then construct exact degree `(8,12)` Weierstrass models,
24 squarefree nodal fibres, the published-R17 frame, and saturated rank-17
section bases.  The resulting transport artifact is
[`elkies-k3-r17-norm12-highest-rank-transports-v1.json`](../artifacts/generated-results/elkies-k3-r17-norm12-highest-rank-transports-v1.json),
SHA-256 `55aea9f4950580f0405e5631bc33abbd2e6c9f81c16c0e5a31c223efb3b09627`.

This is a quotient of the subgroup generated by the displayed public points.
It is not an upper bound on the Mordell--Weil group, an exact-rank theorem, or
a cover-visibility computation.

## Frozen prospective cohort

The prospective commitment is
[`elkies-k3-r17-norm12-prospective-ordinary-family-holdout-v1.json`](../artifacts/generated-results/elkies-k3-r17-norm12-prospective-ordinary-family-holdout-v1.json),
SHA-256 `2e150908cfe7fe72a7157a96aa271bfe7335478708ca0b54e85cc8777f3d5909`.
It contains 1,536 previously unopened parameters: 256 in each of the six
exact rational-`PGL2` classes.  Every parameter lies in the new primitive
projective-height shell

\[
 30001\leq \max(|a|,b)\leq 60000,\qquad b>0,\qquad \gcd(a,b)=1.
\]

Parameters are the first accepted counter-mode SHA-256 draws under the frozen
salt.  Rejection is limited to structural conditions fixed in advance:
out-of-shell or nonprimitive pairs, singular fibres, and duplicate exact
`j`-invariants.  Nagao scores, conductor proxies, public ranks, hit counts,
and cover visibility do not enter selection.  At commitment time every
feature and outcome field is null.

The family-level SHA-256 split locks the complete classes `11952` and `074d9`
as the 512-row outer holdout.  The other four complete classes contain 1,024
prospective development rows.  All 43 chart names are attached to their six
representatives, and the leakage rule covers transformed parameters as well
as literal representative-chart parameters.

## Frozen execution order

The protocol is deliberately sequential:

1. execute all four development families with identical bounds;
2. freeze the complete predictor artifact and its SHA-256;
3. only then open both holdout families, with no retraining, threshold change,
   presieve, adaptive depth, or per-family budget change.

Each row receives exact specialization and MW17 verification, the same fixed
three-block local trace panel, and the same bounded eclib search
(`search_height=12`, `maxr=32`, `pp=0`, 300-second cap).  A positive is kept
only after exact equation transport and finite-quotient escape from the
specialized MW17 subgroup.  A completed miss means only
`BOUNDED_PROTOCOL_NO_GAIN_FOUND`; a timeout or backend failure is censored.

The prospective denominator will therefore be the full scheduled cohort,
with operational outcomes recorded for every row.  It does not retrospectively
identify the unknown denominator behind the 69 public successes.  Whole-family
locking removes direct fibre-level family leakage, but it cannot erase feature
ideas developed before this commitment; that residual limitation must remain
visible in any later predictor report.

## Replay

```sh
sage -python elkies-k3/scripts/plan_r17_norm12_direct_section_basis.sage \
  --source-label norm12-orbit-08234 --height-bound 12 \
  --output artifacts/generated-results/elkies-k3-r17-norm12-08234-direct-section-basis-plan-v1.json

sage -python elkies-k3/scripts/plan_r17_norm12_direct_section_basis.sage \
  --source-label norm12-orbit-07ca9 --height-bound 12 \
  --output artifacts/generated-results/elkies-k3-r17-norm12-07ca9-direct-section-basis-plan-v1.json

sage -python elkies-k3/scripts/compile_r17_norm12_orbit11952_qq.sage \
  --source-label norm12-orbit-08234

sage -python elkies-k3/scripts/compile_r17_norm12_orbit11952_qq.sage \
  --source-label norm12-orbit-07ca9

PYTHONPATH=elliptic-curves/cas sage -python \
  elkies-k3/scripts/certify_r17_norm12_highest_rank_transports.sage --check

.venv/bin/python \
  elkies-k3/scripts/build_r17_norm12_icarm_calibration_dataset.py --check

.venv/bin/python \
  elkies-k3/scripts/build_r17_norm12_prospective_family_holdout.py --check

.venv/bin/python -m unittest \
  elliptic-curves.tests.test_r17_prospective_family_holdout
```
